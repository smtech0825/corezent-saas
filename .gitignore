# 환경 변수 (절대 커밋 금지)
.env
.env.local
.env.production

# Google 서비스 계정 자격증명 JSON (개인 키 포함 — 절대 커밋 금지)
corezent-saas-*.json
*-service-account*.json
gcp-*.json

# 오류 로그
error_log.txt

# OS 생성 파일
.DS_Store
Thumbs.db
desktop.ini

# 에디터 설정
.vscode/settings.json
.idea/
*.swp
*.swo

# 빌드 산출물
dist/
build/
node_modules/
.next/
tsconfig.tsbuildinfo

# Fumadocs 생성물 (content → .source, 빌드 시 자동 생성)
.source/

# Claude 설정
.claude/

# 임시 파일
*.tmp
*.temp

.vercel
txt/
