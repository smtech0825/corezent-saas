/**
 * @파일: lib/quote-constants.ts
 * @설명: 기관 견적 요청 공용 상수 — 폼(클라이언트)과 API(서버)가 같은 값을 쓴다.
 *        한쪽만 바꾸면 안내와 실제 검증이 갈라지므로 반드시 여기서만 정의한다.
 *        (060 마이그레이션의 pc_count CHECK(>= 10)와도 같은 값 — SQL 쪽 주석 참조)
 */

/** 기관 견적 최소 PC 대수 — 미만은 개인 구매 안내 */
export const QUOTE_MIN_PC = 10
