# 작업 인계서 — 2026-08-10

> 내일 이어서 작업할 때 이 문서부터 읽으면 됩니다.
> 오늘 한 일, 지금 멈춰 있는 이유, 내일 무엇부터 할지가 순서대로 적혀 있습니다.

---

## 1. 지금 상태 한 줄

**커밋 10개가 내 컴퓨터에만 있고 아직 실제 사이트에 안 올라갔습니다.** 검증에서 High 문제 3계열이 나와서, 지시대로 올리지 않고 멈춘 상태입니다.

- 브랜치: `main` (로컬 `6b6ba87`)
- 실제 사이트: `fb0e26b` — 오늘 오전 작업까지만 반영돼 있음
- `src/` 미커밋 변경: **0건** (작업은 전부 커밋 완료)
- 그 외 미커밋 45건: `prompts/`·`db-backup/`·`CLAUDE.md` 등 **이전부터 있던 것 — 손대지 않음**

---

## 2. 오늘 실제 사이트에 올라간 것 (완료)

아침~오후에 6개 작업을 배포했습니다. 확인 방법은 `작업확인가이드.md`에 항목 1~26으로 정리돼 있습니다.

1. **화면 레이아웃 정리** — 34개 화면의 여백·폭을 `PageContainer` 하나로 통일
2. **버튼 이름 붙이기** — 아이콘만 있던 버튼 45개에 이름, 탈퇴 버튼 시각 구분, 모바일 ≡ 44px
3. **환불 웹훅 수리 + 실패 메일 알림** — 39건 실패의 원인을 잡고, 실패 시 관리자 메일
4. **관리자 실수 방지** — 권한 드롭다운 등 되돌릴 수 없는 동작에 재확인
5. **구매 후 안내** — 다운로드 진입점, 시작 가이드에 AI 키 등록 단계 추가
6. **기관 도입 페이지 신설** — `/public-sector`, 조달청 번호, 견적 폼

### 대표님이 하셔야 할 입력 (아직 안 하신 것)

- **관리자 → 설정 → 푸터 정보**: 조달청 물품식별번호 `26391406`, 물품분류번호 `43232698`
- **관리자 → 설정 → 푸터 정보**: `개인정보보호책임자: Corezen` → `유승목` 으로 정정
- **관리자 → 설정 → 지원 이메일**: 값이 들어 있는지 확인 (비어 있으면 문의 메일이 안 갑니다)
- **관리자 → 제품 → 지니워크 → 변경 이력**: v0.8.3 추가 + Windows 다운로드 주소 입력
  주소: `https://github.com/smtech0825/GenieWork-releases/releases/download/v0.8.3/GenieWork_0.8.3_x64-setup.exe`
  (v0.5.3의 "최신" 표시는 저절로 꺼집니다 — 그 기능도 오늘 넣었습니다)
- **관리자 → 콘텐츠 → FAQ**: `prompts/draft-faq-public-sector.md` 문안 5개 입력

---

## 3. 올리지 못하고 멈춘 것 (커밋 10개)

### 3-1. 무엇을 고쳤나

**보안 구멍 막기 5개** (`564434d` `698df1a` `bb430de` `1b2fd05` `8f32905`)
- 관리자 서버 기능 33곳에 "당신이 관리자인가" 확인 추가 (전에는 아무나 콘텐츠를 지울 수 있었음)
- 환불 시 결제사가 준 키가 정말 그 주문 것인지 대조한 뒤 정지
- 권한 변경·회원 탈퇴·콘텐츠 삭제가 실패하면 실패라고 알리도록
- 실패 알림 메일이 주문별로 가도록 (전에는 같은 종류면 한 통만)
- 문의 저장이 실패하면 성공이라고 답하지 않도록

**그 위에 후속 5개** (`2186be9` `b75cd24` `c733972` `497b04b` `6b6ba87`)
- 알림이 못 나가면 접수 성공으로 답하지 않도록
- 권한 확인이 막았을 때 화면이 멈추지 않도록 25곳 보강 (`runAdminAction` 공용 장치 신설)
- 알림 메일에서 라이선스 키 값 가리기
- 고객지원 답변이 저장됐을 때만 상태 변경·메일 발송
- **관리자에 문의 열람 화면 신설** (`/admin/inquiries`) — 지금까지 문의를 볼 화면이 아예 없었음

### 3-2. 왜 멈췄나 — 남은 High 3계열

| 문제 | 위치 | 누구 탓인가 |
|---|---|---|
| 콘텐츠 **추가·수정·게시토글**이 DB 오류를 삼켜서, 새로 만든 안전장치가 실패를 성공으로 판정함 | `admin/content/*/page.tsx` (faq:28,39 외 8개 파일) | **기존 문제** (2026-04-08). 후속 Wave 2가 화면만 고쳐 서버가 남음 |
| 라이선스 키가 **서버 로그에 평문**으로 남음 (에러 객체를 통째로 찍는 곳) | `webhooks/route.ts:283,1172` · `_lib_supabase.ts:400,403` | **기존 문제**. 후속 Wave 3이 메일만 가리고 로그를 빠뜨림 |
| 답변은 저장됐는데 상태 변경만 실패하면 예외를 던져 → 관리자 재전송 → **중복 답변·중복 메일** | `admin/support/[id]/page.tsx:87-91` · `ReplyForm.tsx:29-31` | **후속 Wave 4에서 내가 만든 것** |

**중요한 발견 하나**: 후속 Wave 3의 마스킹이 **헛돌았을 가능성이 큽니다.** Postgres 중복키 오류에서 실제 키 값은 `message`가 아니라 `details`에 들어가는데, 제가 가린 건 `message`뿐이었습니다. 정작 키가 새는 곳은 에러 객체를 통째로 찍는 `console.error`입니다.

---

## 4. 내일 할 일 (권장 순서)

### 1순위 — 콘텐츠 서버 기능의 오류 표면화
`admin/content/*/page.tsx` 8개 파일의 **추가·수정·게시토글** 서버 기능이 오류를 삼킵니다.
같은 파일의 **삭제**는 이미 `if (error) throw new Error(...)`로 고쳐져 있으니 **그 방식으로 통일**하면 됩니다.
저장 에디터 3종(히어로·CTA·배너)의 `upsert` 결과 미확인도 같은 계열입니다.
→ 이걸 고쳐야 어제 만든 `runAdminAction` 안전장치가 제 역할을 합니다.

### 2순위 — 중복 답변 위험 제거
`admin/support/[id]/page.tsx`의 상태 변경 실패를 **예외 대신 반환값**으로 바꾸고,
`ReplyForm`이 "답변은 저장됐다"를 구분해 보여주도록. (지금은 무조건 "전송 실패"로 보임)

### 3순위 — 로그의 키 가리기
`console.error(..., 에러객체)` 형태인 곳에 `maskPgUniqueViolation`을 적용.
`_lib_supabase.ts:400`이 `error.message`만 담아 던지는 것도 함께 봐야 합니다.

### 그 뒤 — 재검증 후 배포
`bug-detective` + `code-guardian` 재실행 → High 0건이면 미리보기 브랜치 만들고 `main`에 배포.
**10개 커밋이 한꺼번에 올라갑니다.**

---

## 5. 다음 작업으로 따로 잡아둔 것 (건드리지 말 것)

검증에서 또 나와도 이번 범위가 아닙니다. 목록에만 남깁니다.

- 웹훅 파일이 1,300줄 넘는 것 (규칙은 300줄)
- `any` 타입이 대시보드·결제·라이선스 화면에 있는 것
- `bank-transfer/route.ts:16` 의 하드코딩 이메일
- 기존 서버 기능들의 한국어 설명·반환 타입 누락
- 환불 **발급** 경로에는 주문 대조가 없는 비대칭

---

## 6. Medium 목록 (급하지 않음, 참고)

게시토글 롤백이 연속 클릭에서 어긋날 수 있음 · 토글 버튼에 처리중 비활성화 없음 ·
`/admin/inquiries?page=abc` 같은 값에서 목록이 안 뜸(NaN) · 문의 메일 실패 500이 중복 접수를 만듦 ·
`contact/route.ts:5` 파일 주석이 이번 변경과 반대로 남아 있음 · `runAdminAction`에 `'use client'` 표시 없음 ·
`AboutManager.tsx:174` 히어로 저장만 반환값 미사용 · `/admin/inquiries`에 `metadata` 없음 ·
`maskSecret` 주석의 "원본 길이 유지"가 사실과 다름 ·
**프로덕션에서 서버 예외 메시지가 일반 문구로 치환돼 한국어 사유가 관리자에게 안 보일 수 있음(배포본 실측 필요)**

---

## 7. 되돌리는 방법

```bash
# 후속 5개만 되돌리기
git revert 6b6ba87 497b04b c733972 b75cd24 2186be9

# 보안 5개까지 전부 되돌리기
git revert 6b6ba87 497b04b c733972 b75cd24 2186be9 8f32905 1b2fd05 bb430de 698df1a 564434d
```

아직 안 올라갔으므로 실제 사이트에는 영향이 없습니다.

---

## 8. 지켜온 규칙 (내일도 동일)

- 없는 사실(가격·버전·용량·체험판 조건·처리 시간) 지어내지 않기
- 회귀 위험이 있으면 고치지 말고 보고만
- 범위를 넓히지 않기 — 다른 문제가 보여도 목록에만
- DB 스키마 직접 변경·마이그레이션 실행 금지 (SQL 파일만 만들고 대기)
- `db-backup/` 폴더 미접촉, `git add .` 금지
- 각 단계 끝에서 `npx tsc --noEmit` + `npm run build`, 변경 파일만 개별 커밋
- 보고는 표 없이 쉬운 말로
