# CoreZent 작업 확인 가이드

> 이 파일은 **대표님이 직접 눈으로 확인하기 위한 문서**입니다.
> 개발 용어를 최대한 빼고, "무엇을 고쳤고 / 어디를 열어보면 되고 / 어떻게 보이면 정상인지"만 적습니다.
> 작업이 끝날 때마다 아래에 **새 항목을 계속 덧붙입니다.** (기존 내용은 지우지 않습니다)

## 이 문서를 읽는 법

- ✅ **정상** — 이렇게 보이면 제대로 된 것
- ⚠️ **바뀐 점** — 전과 달라 보이는 게 정상인 것 (놀라지 마세요)
- 🚫 **손 안 댄 것** — 위험해서 일부러 그대로 둔 것

**공통 규칙: 조금이라도 지금 잘 되는 걸 망가뜨릴 위험이 있으면, 고치지 않고 보고만 합니다.**

## 확인은 어디서 하나요

| 상황 | 확인 장소 |
|---|---|
| **실제 사이트** | **https://www.corezent.com** — 로그인해서 보시면 됩니다 |
| 미리보기 (실제 사이트 안 건드리고 미리 볼 때) | `ui/wave1` 같은 작업 브랜치 주소. Vercel 로그인 필요 |

> 각 항목에 **[미리보기]** / **[실제 반영됨]** 표시를 해둡니다.
> **[미리보기]** 면 아직 실제 사이트에는 없습니다. 확인 후 "실제 사이트에 올려줘"라고 하시면 반영합니다.

---

# 2026-08-10 · 화면 레이아웃 정리 (Wave 1)

**상태: [실제 반영됨]** — 2026-08-10 실서비스 배포 완료. www.corezent.com 에서 확인 가능

## 무엇이 문제였나요

메뉴를 눌러 화면을 옮길 때마다 **글자가 시작하는 위치가 좌우로 움찔움찔 움직였습니다.**

공책에 비유하면, 페이지마다 왼쪽 여백을 제각각 다르게 그어놓은 상태였습니다.
어떤 장은 여백 2cm, 어떤 장은 5cm, 어떤 장은 아예 없고. 그래서 장을 넘길 때마다 글이 밀렸습니다.

실제로 이랬습니다.

- 대시보드 6개 화면이 **서로 다른 폭 4가지**를 쓰고 있었습니다
- 관리자 화면 대부분은 **폭 제한이 아예 없어서** 큰 모니터에서 글이 끝까지 늘어졌습니다
- 콘텐츠 편집 화면 9개는 **전부 가운데 정렬이 빠져** 왼쪽에 쏠려 있었습니다
- 스크롤바가 생기는 화면에서는 그것 때문에 또 7px씩 밀렸습니다

## 무엇을 했나요

**"자(ruler)"를 하나 만들어서 모든 화면이 그 자를 쓰게 했습니다.**

`PageContainer`라는 부품을 새로 만들고, 34개 화면이 각자 갖고 있던 제각각의 여백 설정을 지운 뒤
이 부품 하나를 갖다 쓰게 바꿨습니다. 앞으로 폭을 바꾸고 싶으면 **이 부품 한 곳만** 고치면 전부 따라옵니다.

자는 3종류입니다.

| 자 이름 | 어디에 쓰나 | 최대 폭 |
|---|---|---|
| 대시보드용 | 회원이 보는 화면 전부 | 1024px |
| 관리자용 | 관리자 목록·통계 화면 | 1440px |
| 관리자 편집용 | 관리자 콘텐츠 편집 화면 | 768px |

그리고 **스크롤바 자리를 항상 비워두게** 해서, 스크롤바가 생기든 말든 글이 안 밀리게 했습니다.

## 어디서 확인하나요 (실제 사이트)

위험한 순서대로 6개만 봐주시면 됩니다.

### 1. https://www.corezent.com/admin/settings ← 가장 중요

✅ **정상**: 맨 아래 "검색엔진 색인 재요청" 상자의 **왼쪽 끝이, 위쪽 설정 상자들과 일직선**
❌ 문제: 아래 상자만 오른쪽으로 툭 튀어나와 있음

> 이번 작업에서 가장 손이 많이 간 화면입니다. 원래 위/아래가 따로 놀아서 하나로 묶었습니다.

### 2. https://www.corezent.com/dashboard/support

✅ **정상**: 제목 / 문의 작성 상자 / 문의 목록 사이에 **숨 쉴 틈(여백)이 있음**
✅ **정상**: **"새 문의 작성" 상자와 "내 문의 내역" 상자의 좌우 폭이 똑같음**
❌ 문제: 상자들이 서로 딱 붙어 있거나, 위아래 상자 폭이 다름

> 처음에는 글 쓰는 칸을 좁게 뒀는데, 대표님 요청으로 **작성 상자를 키워 아래 목록과 폭을 맞췄습니다.**

### 3. https://www.corezent.com/dashboard/settings

✅ **정상**: 이름·비밀번호 **입력칸 크기는 전과 똑같음**
⚠️ **바뀐 점**: "설정"이라는 **제목만 왼쪽으로 이동**했습니다. 다른 화면과 제목 위치를 맞추기 위해서입니다.
❌ 문제: 입력칸이 화면 끝까지 쭉 늘어나 있음

### 4. https://www.corezent.com (홈) · https://www.corezent.com/pricing

✅ **정상**: 전과 똑같아 보임
❌ 문제: 오른쪽이 잘리거나, 화면 아래에 좌우 스크롤바가 생김

> 스크롤바 처리는 **사이트 전체**에 영향을 줍니다. 그래서 손님이 보는 페이지도 꼭 확인이 필요합니다.

### 5. https://www.corezent.com/admin/users · /admin/orders

✅ **정상**: 표가 전보다 넓어졌지만 **칸이 깨지지 않고** 좌우 스크롤도 안 생김
❌ 문제: 표 칸이 겹치거나 글자가 잘림

### 6. https://www.corezent.com/admin/content/hero

⚠️ **바뀐 점**: 편집 상자가 **조금 넓어지고(672→768px) 화면 가운데로** 이동합니다. 정상입니다.
❌ 문제: 상자가 화면 끝까지 늘어남

## 🚫 일부러 손대지 않은 것 → ✅ 이후 처리 완료

**관리자 상세 화면 3곳** — 회원 상세 / 주문 상세 / 문의 상세

처음에는 위험해서 보류했습니다. 이후 대표님 승인을 받아 **아래 "상세 화면 마무리"에서 처리했습니다.**

## 문제가 생기면

특정 부분만 콕 집어서 되돌릴 수 있게 **5조각으로 나눠 저장**해뒀습니다.

| 증상 | 되돌리는 명령 |
|---|---|
| 손님 페이지가 이상함 | `git revert 1ad52b1` |
| 관리자 설정 화면이 이상함 | `git revert d8c9be7` |
| 관리자 콘텐츠 편집이 이상함 | `git revert 7e1f962` |
| 관리자 목록 화면이 이상함 | `git revert 745b679` |
| 대시보드가 이상함 | `git revert 465ed65` |
| 전부 되돌리고 싶음 | `git revert 1ad52b1 d8c9be7 7e1f962 745b679 465ed65` |

## 이번에 확인한 것 (제가 기계로 검사한 부분)

- 문법 오류 검사 통과 · 전체 빌드 성공 (5단계 각각 실행)
- 34개 화면의 여백 설정이 **전부 같은 값**인지 자동 대조 → 일치
- 가운데 정렬이 빠진 화면 → 0개
- 실제 만들어진 디자인 파일에 새 폭(1440px)이 제대로 들어갔는지 확인 → 들어감

> 다만 **로그인해야 보이는 화면은 기계가 미리 그려볼 수 없습니다.**
> 그래서 위 6개는 대표님 눈으로 봐주셔야 합니다.

---

# 2026-08-10 · 상세 화면 마무리 (Wave 1 추가분)

**상태: [실제 반영됨]** — 2026-08-10 실서비스 배포 완료. www.corezent.com 에서 확인 가능

## 무엇을 했나요

앞에서 위험하다고 보류했던 **관리자 상세 화면 3곳**을 마무리했습니다.

- 회원 상세 (`/admin/users/회원번호`)
- 주문 상세 (`/admin/orders/주문번호`)
- 문의 상세 (`/admin/support/문의번호`)

**바깥 틀만 다른 관리자 화면과 똑같이 맞추고, 안쪽 내용의 폭은 지금 그대로 두었습니다.**

액자에 비유하면, 액자(바깥 틀) 크기는 다른 화면과 통일하고 그 안에 든 사진(본문)은 원래 크기 그대로 둔 것입니다. 사진이 액자 왼쪽에 붙어 있게 해서, 제목과 본문의 왼쪽 끝이 일직선이 되게 했습니다.

## ⚠️ 처음 지시대로 했으면 깨질 뻔했습니다

원래 지시는 "안쪽 상자에 여백 설정을 넣지 마라"였습니다. 그런데 확인해 보니 **그 화면들의 카드 사이 여백은 전부 그 설정 하나가 만들고 있었습니다.** 지시대로 했으면 카드들이 전부 딱 붙어버렸습니다.

그래서 **고치지 않고 먼저 보고**드렸고, 대표님이 "여백 설정은 그대로 옮겨라"로 정정해 주셔서 진행했습니다.
→ **결과적으로 여백은 지금과 1px도 다르지 않습니다.**

## 어디서 확인하나요 (실제 사이트)

### 7. 관리자 → 회원 목록에서 아무 회원이나 클릭

✅ **정상**: 카드들 사이에 **여백이 지금처럼 그대로** 있음
❌ 문제: 카드들이 서로 딱 붙어 있음 ← 이러면 바로 알려주세요

⚠️ **바뀐 점**: 본문이 **화면 가운데 → 왼쪽으로 이동**합니다. 정상입니다.
목록 화면에서 상세 화면으로 넘어갈 때 제목 위치가 안 흔들리게 하려고 일부러 맞춘 것입니다.

### 8. 관리자 → 주문 목록 → 아무 주문이나 클릭

✅ **정상**: 환불·구독취소 버튼이 **그대로 있고 잘 눌림**
❌ 문제: 버튼이 사라졌거나 위치가 이상함

### 9. 관리자 → 문의 목록 → 아무 문의나 클릭

✅ **정상**: 답변 입력칸과 메시지 목록이 **그대로 보임**
❌ 문제: 답변 칸이 안 보임

> 7·8·9는 **"목록 → 상세"로 들어갔다 나왔다** 해보시면 좋습니다.
> 이번 작업의 목적이 바로 그때 글자가 안 흔들리게 하는 것입니다.

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 상세 화면 3곳이 이상함 | `git revert 9f1de6c` |

## 이번에 확인한 것

- 문법 오류 검사 통과 · 전체 빌드 성공
- **감싸기 전/후 화면 부품 개수가 같은지** 확인 → 회원 8개 / 주문 6개 / 문의 4개 (변화 없음)
- **글씨 정리(들여쓰기) 말고 실제로 바뀐 게 없는지** 기계로 대조 → 의도한 3줄 외 변경 0건

---

# 2026-08-10 · 버튼 이름 붙이기 · 위험 버튼 구분 (Wave 2)

**상태: [푸시 전]** — 커밋 4개 완료. 아직 실제 사이트에 없습니다

## 무엇이 문제였나요

**그림만 있고 이름이 없는 버튼이 44개**였습니다.

눈으로 보는 사람은 아이콘 모양으로 짐작할 수 있지만, **화면을 소리로 읽어주는 기기**를 쓰면 "버튼"이라고만 읽고 지나갑니다. 무슨 버튼인지 알 수 없습니다.

그리고 관리자 화면은 강조색이 빨강(인주색)이라 **"저장" 버튼과 "탈퇴" 버튼 색이 거의 똑같았습니다.** 되돌릴 수 없는 버튼이 안전한 버튼처럼 보였습니다.

모바일 햄버거 버튼(≡)은 손가락으로 누르는 영역이 **약 28px** 밖에 안 돼, 권장 크기(44px)에 못 미쳤습니다.

## 무엇을 했나요

1. **아이콘만 있던 버튼 45개에 이름을 붙였습니다.** 문구는 짐작하지 않고 버튼을 눌렀을 때 실제로 실행되는 코드를 하나하나 열어 확인해 정했습니다.
2. **회원 탈퇴 버튼을 눈에 띄게 바꿨습니다.** 아이콘만 있던 것을 **테두리 + 빨간 글씨 "탈퇴"** 로 바꿔, 파란/빨간 채움 버튼(저장·확인)과 확실히 구분됩니다.
3. **햄버거 버튼을 키웠습니다.** 아이콘 크기는 그대로 두고 여백만 늘려 **44×44px** 로 만들었습니다.

## 어디서 확인하나요 (푸시 후)

### 10. 관리자 → 회원 목록

✅ **정상**: 맨 오른쪽 "작업" 칸에 **테두리가 있는 빨간 글씨 "탈퇴" 버튼**이 보임
✅ **정상**: 이미 탈퇴한 회원은 그 버튼이 흐리게 눌리지 않음
❌ 문제: 표 칸이 깨지거나 버튼이 줄바꿈됨

⚠️ **바뀐 점**: 전에는 사람 아이콘만 있었는데 이제 글자가 함께 보입니다. 실수로 누르는 것을 막기 위한 의도된 변경입니다.

### 11. 휴대폰으로 아무 화면이나 접속 → 왼쪽 위 ≡ 버튼

✅ **정상**: 버튼이 전보다 커져서 **누르기 편함**
❌ 문제: 옆의 "관리자 패널" / "대시보드" 글자가 잘림

### 12. 아무 아이콘 버튼에나 마우스를 올려보기

✅ **정상**: 잠깐 기다리면 **설명 글이 뜸** (예: 연필 = "FAQ 수정")
❌ 문제: 설명 글이 **두 개 겹쳐서** 뜸 ← 이러면 알려주세요

## 🚫 일부러 손대지 않은 것

- **탈퇴 확인 창의 빨간 "확인" 버튼** — 옆의 "취소"가 이미 테두리형이라, 확인까지 테두리형으로 바꾸면 둘이 비슷해져 **잘못 누를 위험**이 있습니다. 지금이 더 안전합니다.
- **사진 위의 작은 빨간 X 버튼** (소개 이미지·후기 사진) — 사진 위에 얹혀 있어 채움 빨강이라야 잘 보입니다.
- **아이콘 버튼 여백 일괄 변경** — 지시는 "p-2로 통일"이었지만 실제로 세어 보니 현재 다수가 다른 값이었습니다. 섞여 있던 한 줄만 맞추고 나머지는 그대로 뒀습니다.

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 버튼 설명 글이 이상함 | `git revert b71d705` |
| 회원 목록 탈퇴 버튼이 이상함 | `git revert 0407210` |
| 회원 목록 아이콘 간격이 이상함 | `git revert 4bd5908` |
| 모바일 ≡ 버튼이 이상함 | `git revert 75c698c` |

## 이번에 확인한 것

- 문법 오류 검사 통과 · 전체 빌드 성공 (4단계 각각 실행)
- 이름 없는 아이콘 버튼 개수 → **0개** (작업 전 44개)
- 설명 글이 두 개 겹칠 위험이 있는 버튼 → **0개**
- 햄버거 버튼 크기 계산 → 아이콘 20px + 여백 12px×2 = **44px**

---

# 2026-08-10 · 환불 웹훅 수리 · 실패 메일 알림

**상태: [미리보기]** — 확인 후 실제 사이트 반영 예정

## 무엇이 문제였나요

**환불 알림이 39번 연속으로 실패해 있었습니다.** 2026년 7월 29일, 83초 동안 13개 주문에 대해 같은 오류가 39번 쌓였습니다.

원인은 코드가 "환불된 주문을 반드시 찾을 수 있다"고 가정한 것이었습니다. 못 찾으면 오류를 내고 멈추는데, Lemon Squeezy는 실패하면 계속 다시 보냅니다. 그래서 같은 실패가 무한 반복됐습니다.

왜 못 찾았는지도 확인했습니다. **7월 4일 출시 전 테스트 정리로 주문 기록을 지우신 것**이 원인이고, 사고가 아닙니다. 다만 그 뒤에 들어온 환불 알림이 지워진 주문을 찾다가 죽은 것입니다.

그리고 **이 실패가 39번 쌓이는 동안 아무 알림도 가지 않았습니다.** 관리자 화면에 직접 들어가 모니터링 로그를 열어봐야만 보였습니다.

## 무엇을 했나요

환불 알림이 주문을 못 찾아도 **더 이상 죽지 않습니다.** 경고만 남기고 정상적으로 마칩니다. 못 찾은 주문은 다시 시도해도 영원히 찾을 수 없으니, 반복해서 재시도하게 두는 것이 무의미하기 때문입니다.

주문 기록이 없어도 **환불된 라이선스 키는 정지시킵니다.** Lemon Squeezy에 주문번호로 물어보면 그 주문의 키를 알려주므로, 그 키를 찾아 라이선스 서버에서 사용 중지합니다.

**웹훅이 실패하면 이제 메일이 옵니다.** 받는 주소는 관리자 설정의 "지원 이메일"을 그대로 씁니다. 메일에는 어떤 알림이 언제 왜 실패했고 어느 주문인지가 들어갑니다.

**메일 폭주는 막았습니다.** 같은 종류의 실패는 30분에 한 통만 갑니다. 이번처럼 83초에 39번 실패해도 메일은 한 통입니다.

## 어디서 확인하나요

### 13. 관리자 → 설정

✅ **정상**: "지원 이메일" 칸에 대표님이 받으실 주소가 들어 있음
❌ 문제: 비어 있음 ← 비어 있으면 실패 메일이 발송되지 않습니다. 꼭 채워 주세요.

### 14. 관리자 → 모니터링 로그

✅ **정상**: 기존 40건 기록이 **그대로 남아 있음** (지우지 않았습니다)
⚠️ 앞으로 환불 알림이 주문을 못 찾으면 여기에 `order_refunded_order_not_found` 로 남습니다. 이건 오류가 아니라 경고이며 메일도 가지 않습니다.

> 실제 결제·환불·메일 발송 테스트는 하지 않았습니다. 코드 읽기와 빌드로만 검증했습니다.

## 🚫 손대지 않은 것

- **7월 4일 백업 폴더(db-backup/)** — 그대로 뒀습니다
- **기존 로그 40건** — 한 줄도 지우거나 고치지 않았습니다
- **주문 중복 방지 코드** — 이미 2026-07-03에 고쳐져 있어 손댈 것이 없었습니다
- **한 주문에 구독이 2건 이상일 때 만료일 계산** — 범위 밖으로 지시받아 그대로 뒀습니다

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 환불 처리가 이상함 | `git revert 7a04aa4` |
| 실패 메일이 너무 자주 옴 | `git revert cde318a` |

---

# 2026-08-10 · 관리자 실수 방지 · 자잘한 오류 정정

**상태: [미리보기]** — 확인 후 실제 사이트 반영

## 무엇을 했나요

**홈 "도입 절차" 제목의 숫자를 맞췄습니다.** 제목은 "네 단계"인데 실제 표에는 6단계가 나열돼 있었습니다. 이제 제목의 숫자를 표의 실제 줄 수에서 만들어내므로, 관리자 화면에서 단계를 추가하거나 지워도 다시 어긋나지 않습니다. 단계 내용 자체는 건드리지 않았습니다.

**문의 알림 받는 주소를 설정에서 읽게 했습니다.** 지금까지는 코드에 주소가 박혀 있었습니다. ⚠️ **받는 곳이 `support@corezent.com` 으로 바뀝니다.** 설정의 "지원 이메일" 값을 쓰기 때문입니다. 다른 주소로 받으시려면 관리자 → 설정에서 바꾸시면 됩니다.

**관리자 화면에서 되돌릴 수 없는 동작에 재확인을 넣었습니다.** 특히 **권한 드롭다운**은 지금까지 클릭 한 번에 바로 바뀌었습니다. 이제 물어보고, 취소하면 드롭다운이 원래 값으로 되돌아갑니다. 제휴 화면의 크레딧 전환·할인코드 발급에도 넣었습니다.

**기존 확인 문구도 구체적으로 바꿨습니다.** "이 FAQ를 삭제할까요?" 처럼 어느 것인지 모르던 문구를, 실제 제목과 "되돌릴 수 없습니다"를 넣어 다시 썼습니다.

## 어디서 확인하나요

### 15. 홈 → 도입 절차

✅ **정상**: 제목이 **"다운로드에서 사용까지, 6단계"** 이고 아래 표도 6줄

### 16. 관리자 → 회원 목록 → 역할 드롭다운을 "관리자"로 바꿔보기

✅ **정상**: 확인 창이 뜨고, **취소하면 드롭다운이 "사용자"로 되돌아감**
❌ 문제: 취소했는데 드롭다운이 "관리자"로 남아 있음 ← 이러면 알려주세요

### 17. 관리자 → 콘텐츠 → FAQ에서 휴지통 눌러보기 (취소하세요)

✅ **정상**: 확인 창에 **그 FAQ의 실제 질문 내용**이 보이고 "되돌릴 수 없습니다" 안내가 있음

### 18. 관리자 → 설정 → 지원 이메일

✅ **정상**: 문의를 받으실 주소가 들어 있음. 문의 알림이 이 주소로 갑니다.

## 🚫 손대지 않은 것

- **푸터 "개인정보보호책임자: Corezen"** — 관리자 설정에서 편집하는 값이고, 그 자리에는 **사람 이름**이 들어가야 해 보입니다. 개인정보처리방침 페이지에는 "유승목"으로 적혀 있습니다. 제가 임의로 정할 일이 아니라 그대로 뒀습니다. **아래 "대표님이 고치실 것" 참고.**
- **문의 폼의 Website 칸** — 스팸 봇을 걸러내는 미끼 칸이고 이미 화면 밖으로 숨겨져 있었습니다. 지우면 스팸이 들어옵니다.
- **회원 탈퇴 처리 방식** — 이미 기록을 지우지 않고 "비활성"으로만 바꾸는 방식이었습니다. 고칠 것이 없었습니다.

## ✍️ 대표님이 직접 고치실 것

**관리자 → 설정 → 푸터 정보**에서 아래 한 곳을 고쳐 주세요.

- 지금: `개인정보보호책임자: Corezen`
- 바꿀 값: `개인정보보호책임자: 유승목` (개인정보처리방침 페이지와 맞추는 경우)

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 홈 단계 숫자·문의 메일이 이상함 | `git revert ba8f35e` |
| 관리자 확인 창이 이상함 | `git revert 07c6384` |

---

# 2026-08-10 · 구매 후 안내 (다운로드 · 시작 가이드)

**상태: [미리보기]** — 확인 후 실제 사이트 반영

## 먼저: 대표님이 넣으셔야 할 것

**지니워크 설치 파일 주소가 DB에 아직 없습니다.** 버전 12개(v0.4.1~v0.5.3) 전부 주소 칸이 비어 있어, 구매자가 다운로드 버튼을 볼 수 없었습니다.

관리자 → 제품 → 지니워크 → 변경 이력 → **버전 추가**에서 아래를 넣어 주세요.

- 버전: `v0.8.3` / 릴리스 날짜: 실제 배포일 / "최신으로 표시" **켜기**
- 다운로드 URL의 **Windows 칸**에 받으신 주소를 붙여넣기
- 나머지 칸(macOS·Linux 등)은 비워 두기

⚠️ **그다음 v0.5.3을 수정해 "최신으로 표시"를 꺼주세요.** 새 버전을 최신으로 켜도 이전 것이 자동으로 꺼지지 않습니다. 둘 다 켜져 있으면 어느 쪽이 보일지 알 수 없습니다.

주소를 넣으시면 **코드 수정 없이 바로** 다운로드 버튼이 나타납니다.

## 무엇을 했나요

**다운로드 버튼이 왜 없는지 알려줍니다.** 지금까지는 주소가 없으면 버튼이 조용히 사라져서, 구매자는 "내 화면만 안 보이나" 싶었습니다. 이제 "설치파일 준비 중"이라고 표시됩니다.

**시작 가이드에 빠진 단계를 채웠습니다.** 사용설명서를 확인해 보니 처음 쓸 때 세 가지(설치 → 라이선스 인증 → AI 키 등록)가 필요한데, 대시보드 안내에는 **AI 키 등록이 빠져 있었습니다.** 이걸 넣고, 설치·인증 설명도 설명서 내용에 맞춰 구체적으로 바꿨습니다. 인증 단계 링크는 라이선스 키를 복사할 수 있는 화면으로 연결했습니다.

## 어디서 확인하나요

### 19. 대시보드 → 라이선스

✅ **정상**: 다운로드 칸에 **"설치파일 준비 중"** 이 보임 (주소를 넣으시면 다운로드 버튼으로 바뀝니다)

### 20. 대시보드 개요 (라이선스가 있는 계정)

✅ **정상**: "시작하기" 상자에 **5단계**가 보이고 그중 **"AI 키 등록"** 이 있음
✅ **정상**: "라이선스 인증" 단계의 링크가 **"내 라이선스 키 복사하기"** 로 바뀜

## 🚫 넣지 않은 것 (확인되지 않아서)

- **파일 용량** — DB의 용량 칸이 전 건 비어 있습니다
- **체험판 조건** — 사용설명서에 체험판 언급이 아예 없습니다
- **지니워크 안내 문서 링크** — 제품 정보의 안내 링크가 비어 있습니다(지니스톡·지니포스트만 있음)

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 라이선스 화면 다운로드 칸이 이상함 | `git revert b7fb744` |
| 시작하기 체크리스트가 이상함 | `git revert 5a392c7` |

---

# 2026-08-10 · 개요 분기 · 주문 상태 안내 · 최신 표시 자동 해제

**상태: [미리보기]** — 확인 후 실제 사이트 반영

## 무엇을 했나요

**개요 화면이 사람마다 다르게 보입니다.** 지금까지는 아직 안 산 사람과 산 사람에게 똑같이 보였습니다. 이제 상황에 맞는 안내가 맨 위에 뜹니다. 아직 안 사신 분에게는 제품 둘러보기, 입금 확인을 기다리는 분에게는 입금 안내, 라이선스가 만료·회수된 분에게는 상태 확인 링크가 보입니다. **정상적으로 쓰고 계신 분에게는 아무 배너도 뜨지 않습니다.** 기존 "시작하기" 안내와 겹치지 않게 하기 위해서입니다.

**주문마다 다음에 무슨 일이 일어나는지 한 줄로 알려줍니다.** 계좌이체로 주문하신 분이 "입금 대기"라는 딱지만 보고 그다음을 몰랐습니다. 이제 "입금이 확인되면 라이선스가 발급됩니다" 같은 안내가 딱지 아래에 붙습니다. **처리 시간은 확인된 안내 문구가 없어 언급하지 않았습니다.**

**버전을 "최신"으로 저장하면 이전 최신이 자동으로 꺼집니다.** 앞서 보고드린 문제입니다. 이제 v0.8.3을 추가하시면 v0.5.3의 최신 표시가 저절로 꺼집니다. 다만 **이미 둘 다 켜져 있는 기존 데이터는 건드리지 않았고**, 화면 쪽에도 정렬을 넣어 그런 경우에도 릴리스 날짜가 가장 늦은 버전이 잡히게 했습니다.

## 어디서 확인하나요

### 21. 대시보드 개요 — 계정 상태별

✅ **정상**: 라이선스가 정상인 계정 → **배너 없이** "시작하기" 상자만 보임
✅ **정상**: 라이선스가 없는 계정 → "아직 구매하신 제품이 없습니다" + 제품 둘러보기 버튼
❌ 문제: 안내가 두 개 겹쳐서 보임

### 22. 대시보드 → 결제

✅ **정상**: 각 주문의 상태 딱지 아래에 **다음 단계 한 줄**이 보임 (결제 완료된 주문은 안내 없음이 정상)

### 23. 관리자 → 제품 → 지니워크 → 변경 이력

✅ **정상**: 새 버전을 "최신으로 표시" 켜고 저장하면, **이전 버전의 최신 표시가 저절로 꺼짐**

## 🚫 하지 않은 것

- **없는 통계 숫자 만들기** — 새로 조회한 값(활성 라이선스 수, 입금대기 주문 수)은 화면에 숫자로 표시하지 않고 어떤 안내를 띄울지 고르는 데만 썼습니다
- **기존 카드 삭제** — 통계 카드·구독·주문 목록은 그대로 두고 배너만 위에 얹었습니다
- **기존 데이터 수정** — 최신 표시가 둘 다 켜진 과거 데이터는 손대지 않았습니다

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 개요 화면 배너가 이상함 | `git revert b7f8779` |
| 결제 화면 안내 문구가 이상함 | `git revert 326a4b1` |
| 변경 이력 저장이 이상함 | `git revert 7bb44d1` |

---

# 2026-08-10 · 기관 도입 페이지 신설

**상태: [미리보기]** — 확인 후 실제 사이트 반영

## 무엇을 했나요

**기관 도입 안내 페이지를 새로 만들었습니다.** 공무원이 부서 예산으로 도입을 검토할 때 볼 것이 사이트에 하나도 없었습니다. 이제 대수별 요금, 조달청 등록번호, 구매 절차, 견적 요청 폼이 한 페이지에 있습니다.

**요금표는 관리자에 등록된 옵션을 그대로 읽어 그립니다.** PC 대수를 코드에 적어두지 않았기 때문에, 옵션을 추가하거나 금액을 바꾸시면 이 페이지도 저절로 따라갑니다.

**견적 요청은 기존 문의 시스템을 그대로 씁니다.** 새 저장소를 만들지 않았고, 받으시는 방식도 지금 문의와 같습니다.
> ⚠️ 2026-08-13부터 바뀌었습니다 — 이제 견적 요청은 전용 저장소에 쌓이고 관리자 → 견적 요청에서 보고 견적서 PDF까지 발급합니다. 아래 "2026-08-13 (3차)" 항목을 보세요.

**조달청 번호 입력칸을 만들었습니다.** 관리자 → 설정 → 푸터 정보에 있습니다. 넣으시면 푸터와 기관 도입 페이지에 나타나고, 비워 두면 그 줄이 아예 안 보입니다.

## ✍️ 대표님이 넣으셔야 할 것

**관리자 → 설정 → 푸터 정보**에서 아래 두 칸을 채워 주세요. 넣기 전까지는 화면에 나오지 않습니다.

- 조달청 물품식별번호: `26391406`
- 조달청 물품분류번호: `43232698`

**관리자 → 콘텐츠 → FAQ**에는 `prompts/draft-faq-public-sector.md` 파일의 문안을 보고 5개를 추가해 주세요. 보안 감사·내부망·개인 구매·AI 키·환불에 대한 답변이고, 각 항목마다 근거를 적어뒀습니다.

## 어디서 확인하나요

### 24. 상단 메뉴 → 기관 도입

✅ **정상**: 대수별 요금표에 1PC·3PC·5PC·10PC가 월간·연간으로 보임
✅ **정상**: 조달청 번호를 아직 안 넣으셨으면 **그 칸이 아예 안 보임** (빈칸이 아니라 없음)

### 25. 요금 페이지 맨 아래

✅ **정상**: "기관 예산으로 도입하시나요?" 안내와 버튼이 보임

### 26. 아무 페이지나 맨 아래(푸터)

✅ **정상**: 조달청 번호를 넣으시면 저작권 줄 아래에 나타남. 안 넣으셨으면 안 보임

## 🚫 넣지 않은 것

- **공공기관 수량형 요금(PC당 연 79,000원, 최소 10PC)** — 지금 옵션 구조로는 표현할 수 없습니다. 아래 설명 참고
- **법령 조문·금액 기준** — 정확한 조문을 확인할 출처가 없어 절차만 설명했습니다
- **세금계산서 발급 가능 여부** — 결제대행이 해외 사업자라 확인이 안 됐습니다
- **처리 시간 약속** — 확인된 안내 문구가 없습니다
- **보안 인증·준수 주장** — 인증 취득 여부가 확인되지 않았습니다

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 기관 도입 페이지가 이상함 | `git revert 9c2047f` |
| 푸터·요금 페이지가 이상함 | `git revert 94d60f7` |

---

---

# 2026-08-12 · 로그인 안내·복사 버튼 마무리 4건 (+ 세금 계산기 동시 배포)

**상태: [실제 반영됨]** — 2026-08-12 실서비스 배포 완료. www.corezent.com 에서 확인 가능
**미리보기**: https://corezent-saas-git-preview-auth-leftovers-20260812-core-zent.vercel.app (Vercel 로그인 필요)

> 이번 배포에는 어제까지 쌓여 있던 커밋 전부(인증·화면 정리 10개 + 세금 계산기 7개)와
> 오늘 마무리 6개가 함께 올라갔습니다. **세금 계산기 화면은 다른 세션 담당이라
> 이 문서의 확인 목록에서 뺐습니다.**

## 무엇을 고쳤나요

1. **복사 버튼이 실패를 알려줍니다.** 지금까지는 복사가 안 돼도(권한 거부 등) 화면에 아무
   표시가 없었습니다. 이제 실패하면 버튼이 빨간 X와 "복사 실패"로 잠시(4초) 바뀝니다.
2. **만료된 메일 링크와 소셜 로그인 취소를 정확히 구분합니다.** 두 경우가 같은 오류 코드로
   올 수 있어서, 오류에 딸려 오는 설명 문장까지 함께 보고 각각 맞는 안내를 보여줍니다.
3. **로그인 후 "돌아갈 경로" 흔적을 어디서든 지웁니다.** 전에는 특정 화면에 도착할 때만
   지워서, 흔적이 최대 10분 남아 다음 로그인이 엉뚱한 곳으로 갈 수 있었습니다.
4. **수신 설정 저장이 실패하면 이유가 기록에 남습니다.** (화면 문구는 그대로)

## 확인 목록 — 위험한 순서대로

### 1. 로그인 자체 (가장 중요 — 모든 요청이 지나가는 부분을 손댔습니다)

- www.corezent.com/auth/login 에서 **이메일+비밀번호 로그인** → 정상 로그인되고 이동
- **카카오/네이버/구글 중 하나로 소셜 로그인** → 정상 로그인
- ✅ **정상**: 평소와 똑같이 로그인되면 통과. 로그인이 안 되거나 이상한 곳으로 가면 문제

### 2. 로그인 후 이동

- **로그아웃 상태**에서 www.corezent.com/dashboard 주소를 직접 입력 → 로그인 화면이 뜨고,
  로그인하면 **대시보드로 돌아오는지**
- **로그인 상태**에서 www.corezent.com/auth/login 주소를 직접 입력 → **대시보드로 자동 이동**하는지
- ✅ **정상**: 둘 다 매끄럽게 이동하면 통과

### 3. 복사 버튼 3곳

- 대시보드 → 라이선스: 키 옆 복사 버튼 → ✅ 체크 표시 후 메모장에 붙여넣어 **전체 키**가 들어갔는지
- 대시보드 → 제휴: "복사" 버튼 → "복사됨"으로 바뀌는지
- 관리자 → 상품 → 옵션표의 URL 복사
- ⚠️ **바뀐 점**: 복사가 실패하는 환경(드묾)에서는 빨간 X와 "복사 실패"가 잠깐 보입니다.
  이건 고장이 아니라 **원래 조용히 넘어가던 실패를 이제 보여주는 것**입니다

### 4. 설정 화면

- 대시보드 → 설정: 이름 저장, 알림 수신 토글 켰다 껐다 → 저장 안내가 뜨는지
- ✅ **정상**: "프로필이 업데이트되었습니다" / "수신 동의로 저장되었습니다" 안내

### 5. 만료 메일 링크 안내 (재현이 번거로우면 건너뛰셔도 됩니다)

- 예전에 받은(만료된) 인증 메일 링크 클릭 → "인증 메일을 다시 받아 주세요" 계열 안내
- 소셜 로그인 동의창에서 **취소** 클릭 → "소셜 로그인을 마치지 못했습니다…" 안내
- ✅ **정상**: 각각 위 문구. 영어 오류 문장이 그대로 보이면 문제

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 복사 버튼이 이상함 | `git revert --no-edit 727d164` |
| 로그인 오류 안내가 이상함 | `git revert --no-edit 1dad692 ba3451b` |
| 로그인 후 이동이 이상함 | `git revert --no-edit 3357b81 a403e55` |
| 오늘 마무리 6개 전부 | `git revert --no-edit 3357b81 1dad692 22074bb a403e55 ba3451b 727d164` |

---

# 2026-08-12 · 통계 카드·빈 안내 모양 통일 (카드 정리 5단계)

**상태: [실제 반영됨]** — 2026-08-12 실서비스 배포 완료. www.corezent.com 에서 확인 가능
**미리보기**: https://corezent-saas-git-preview-ui-card-consistency-850c9b-core-zent.vercel.app (Vercel 로그인 필요)

## 무엇을 고쳤나요

1. **"~가 없습니다" 안내가 어디서든 같은 모양입니다.** 화면마다 여백이 제각각이던 것을
   부품 하나로 통일했습니다(22곳). 개요의 빈 카드는 문구가 위쪽에 어정쩡하게 뜨던 것을
   세로 가운데로 옮겼습니다.
2. **"0 ↘100%" 같은 억지 증감률이 사라졌습니다.** 비교할 지난 값이 없거나 이번 달 값이
   아직 0이면 % 대신 – 로 표시합니다(관리자 개요).
3. **숫자 카드가 한 종류가 됐습니다.** 관리자 개요·매출 리포트·대시보드·제휴의 카드
   20곳이 같은 모양(아이콘 위 → 숫자 → 이름)입니다. 보조 수치가 없는 카드는 빈 공간을
   남기는 대신 카드가 작아집니다. 대시보드의 "0 · 전체 라이선스" 한 줄 표기는 숫자와
   이름 두 줄로 바뀌었습니다.
4. **매출 리포트가 정돈됐습니다.** 넓은 화면에서 카드가 4~6열로 모이고, 매출이 하나도
   없으면 차트 대신 "아직 매출 데이터가 없습니다"가 나옵니다(예전에는 0원인데도 바닥에
   막대 조각 12개가 그려졌습니다). 데이터가 있으면 왼쪽 눈금과 막대 위 금액이 보입니다.
5. **제휴 화면의 지표 묶음에 "추천 성과" 제목이 생겨** 아래 적립 현황과 별개 블록으로
   읽힙니다.

## 확인 목록 — 위험한 순서대로

### 1. 관리자 개요 (가장 많이 바뀐 화면)

- www.corezent.com/admin — 카드 4개가 같은 모양(아이콘 위·숫자·이름)인지
- ✅ **정상**: 총 사용자·총 매출 카드 아래에 월간/연간 줄이 있고, 활성 라이선스·열린
  티켓 카드는 **아래가 빈 채 길지 않고 짧게 끝남** (빈 공간이 없는 게 정상)
- ✅ **정상**: 열린 티켓 카드의 아이콘을 누르면 고객지원 화면으로 이동
- ⚠️ **바뀐 점**: 증감률에 –(대시)가 보이면 "비교할 지난 값이 없다"는 뜻입니다. 고장 아님

### 2. 관리자 매출 리포트

- www.corezent.com/admin/revenue — 카드 6개 모양·차트
- ✅ **정상**: 매출이 있으면 차트 왼쪽에 금액 눈금, 막대 위에 축약 금액(예: 129만)
- ✅ **정상**: 매출이 하나도 없으면 차트 대신 "아직 매출 데이터가 없습니다"

### 3. 대시보드 개요·라이선스·결제 (손님 화면)

- 대시보드 첫 화면 — 카드 3개가 숫자·이름 두 줄인지, 구독·주문이 없을 때 안내가
  카드 세로 가운데에 오는지
- 라이선스·결제 화면 — 목록이 없을 때 안내 카드가 같은 모양인지
- 🚫 **손 안 댄 것**: 상태 안내 배너와 "시작하기" 체크리스트는 그대로입니다

### 4. 제휴 화면

- 대시보드 → 제휴 — 위 지표 3개에 "추천 성과" 제목, 아래 "적립 현황"과 구분되는지
- ✅ **정상**: 카드가 다른 화면과 같은 모양. 잔액 카드(큰 파란 테두리)는 일부러 그대로

### 5. 관리자 나머지 목록 화면 (가볍게)

- 주문·사용자·라이선스·고객지원·문의·로그 — 목록이 비었을 때 안내가 같은 모양인지

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 빈 안내 카드가 이상함 | `git revert --no-edit 6c175b4 cd782c2` |
| 증감률 표시가 이상함 | `git revert --no-edit 30ebcf0` |
| 숫자 카드가 이상함 | `git revert --no-edit 36c7ffb c0f4ac4` |
| 매출 리포트·차트가 이상함 | `git revert --no-edit 1a80b31` |
| 제휴 화면이 이상함 | `git revert --no-edit ef69744` |
| 이번 작업 전부(7개) | `git revert --no-edit 36c7ffb 6c175b4 ef69744 1a80b31 c0f4ac4 30ebcf0 cd782c2` |

---

# 2026-08-12 · 입력칸·버튼 정리 (드롭다운·저장 버튼·설정 화면)

**상태: [실제 반영됨]** — 2026-08-12 실서비스 배포 완료. www.corezent.com 에서 확인 가능
**미리보기**: https://corezent-saas-git-preview-ui-inputs-20260812-core-zent.vercel.app (Vercel 로그인 필요)

## 무엇을 고쳤나요

1. **드롭다운(고르는 칸)이 사이트 모양이 됐습니다.** 브라우저 기본 모양 그대로라 낡아
   보이던 12곳을 공용 부품으로 통일했습니다. 고르는 동작(키보드·읽어주는 기기 포함)은
   브라우저 기본 그대로입니다.
2. **저장 안 했는데 버튼에 붙어 있던 체크(✓)가 사라졌습니다.** 콘텐츠 관리·제휴 설정·
   정산 계좌의 저장·추가 버튼 19곳. 이제 체크는 저장이 끝났을 때만 잠깐 보입니다.
3. **회원 목록의 역할이 한 칸이 됐습니다.** "역할" 배지와 "역할 변경" 드롭다운이 같은
   값을 두 번 보여주던 것을 드롭다운 하나로 합쳤습니다. 바꿀 때 재확인 창, 취소하면
   원래대로, 실패하면 알림 — 전부 그대로입니다.
4. **주문 화면의 검색·필터·내보내기가 한 줄이 됐습니다.**
5. **설정 화면**: 값을 바꾸고 저장 안 한 카드에 "저장 안 됨" 표시가 뜹니다. SMTP
   포트·암호화 칸이 휴대폰에서 찌그러지던 것과 좌우 열 높이 어긋남도 정리했습니다.
6. **대시보드 개요의 "제품 둘러보기" 중복**: 빈 카드 2개에서 링크를 빼고 배너에만
   남겼습니다(결정하신 방식).

## 확인 목록 — 위험한 순서대로

### 1. 관리자 → 사용자 관리 (동작을 지켜야 하는 부분)

- 아무 회원의 역할 드롭다운을 "관리자"로 바꿔보기 → **확인 창**이 뜨는지
- 확인 창에서 **취소** → 드롭다운이 **원래 값으로 돌아오는지**
- ✅ **정상**: 확인 창 → 취소 시 원상복구. 바뀐 건 배지 열이 없어진 것뿐

### 2. 관리자 → 설정

- 아무 카드나 값을 바꾸면 그 카드 제목 옆에 **"저장 안 됨"** 이 뜨고, 저장하면 사라지는지
- 저장 버튼: 평소엔 "저장" 글자만, 저장 후 3초간 체크와 "저장됨!"
- 휴대폰(또는 창을 좁게)에서 SMTP 포트·암호화가 세로로 쌓이는지
- ✅ **정상**: 위 세 가지 모두. 카드 순서가 바뀐 것(계좌이체가 SMTP 앞)은 의도입니다

### 3. 관리자 → 주문

- 검색창·상태·날짜·CSV 내보내기가 **한 줄**에 있고, 검색·필터가 평소처럼 걸리는지
- 상태 드롭다운이 다른 입력칸과 같은 모양(회색 테두리·작은 화살표)인지

### 4. 관리자 → 콘텐츠 관리 (아무 화면)

- 저장·추가 버튼에 평소 체크(✓)가 **없는지** (저장 동작은 그대로)

### 5. 대시보드 (손님 화면)

- 개요: 구독·주문이 없을 때 안내 문구만 있고 "제품 둘러보기 →" 링크는 **배너에만** 있는지
- 제휴 → 정산 계좌의 은행 선택, 고객지원 문의 폼의 우선순위 드롭다운이 새 모양인지

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 드롭다운이 이상함 | `git revert --no-edit eb0204a ad5089b` |
| 저장 버튼·체크가 이상함 | `git revert --no-edit 20ee784 323b2cb` |
| 회원 역할 칸이 이상함 | `git revert --no-edit ad390ac` |
| 주문 화면이 이상함 | `git revert --no-edit c626f3f` |
| 설정 "저장 안 됨"이 이상함 | `git revert --no-edit eb0204a 02255a9` |
| 개요 링크가 이상함 | `git revert --no-edit 89f8cf1` |
| 이번 작업 전부(8개) | `git revert --no-edit eb0204a 89f8cf1 02255a9 c626f3f ad390ac ad5089b 20ee784 323b2cb` |

---

# 2026-08-12 · 사이드바·상단 헤더 정리

**상태: [실제 반영됨]** — 2026-08-12 실서비스 배포 완료. www.corezent.com 에서 확인 가능
**미리보기**: https://corezent-saas-git-preview-ui-sidebar-20260812-core-zent.vercel.app (Vercel 로그인 필요)

## 무엇을 고쳤나요

1. **"관리자 패널" 이중 표기 제거** — 사이드바 로고 아래만 남기고 상단 바 배지를 없앴습니다.
2. **관리자 메뉴 그룹 3개(관리자·프론트엔드·시스템)가 전부 접힙니다.** 접어 둔 상태는
   기억되고, 접힌 그룹 안의 화면으로 가면 자동으로 펼쳐집니다. 그룹을 접었을 때 고객지원
   미읽음 알림이 있으면 그룹 제목 옆에 빨간 점이 옮겨 붙습니다.
3. **노트북 화면 잘림** — 평소 상태(프론트엔드 접힘)에서 768px 화면에 다 들어가게
   높이를 줄였고(휴대폰 터치 크기는 유지), 그래도 스크롤이 생기면 위아래 흐린 그림자로
   "더 있다"를 알립니다.
4. **"관리자 페이지로 이동"이 보통 색**이 됐습니다. 빨간색은 로그아웃만 씁니다.
5. **대시보드 상단 헤더에 현재 화면 이름**이 나옵니다(사이드바 메뉴 이름과 동일).
6. 메뉴 아이콘 선을 한 단계 굵게 해 로고와의 무게 차이를 줄였습니다.

## 확인 목록 — 위험한 순서대로

### 1. 관리자 어느 화면이든 (사이드바는 모든 화면에 나옵니다)

- www.corezent.com/admin — 메뉴가 평소처럼 다 보이고 눌리는지
- 그룹 제목(관리자/프론트엔드/시스템)을 눌러 접었다 펴기 → 새로고침해도 기억되는지
- "관리자" 그룹을 접은 채 설정 화면에 가 보기 → 미읽음 티켓이 있으면 그룹 제목 옆 빨간 점
- ✅ **정상**: 접힘·기억·자동 펼침·빨간 점 네 가지 모두. 상단 바의 "관리자 패널" 배지가
  없어진 것은 의도입니다(로고 아래에 있음)

### 2. 대시보드 (손님 화면)

- 라이선스·결제 등으로 옮겨 다니면 위쪽 헤더에 화면 이름이 사이드바와 똑같이 나오는지
- 사이드바의 "관리자 페이지로 이동"이 회색, "로그아웃"만 빨간색인지
- ✅ **정상**: 두 가지 모두. 눌리는 곳(가는 곳)은 전과 동일

### 3. 노트북·휴대폰

- 노트북(작은 화면)에서 관리자 메뉴가 평소 상태에서 잘리지 않는지, 전부 펼치면
  스크롤 위아래에 흐린 그림자가 보이는지
- 휴대폰에서 햄버거 메뉴 열고 그룹 접기·메뉴 누르기가 편한지(터치 크기 유지)

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 그룹 접힘·배지·아이콘이 이상함 | `git revert --no-edit 5a707bb 68c23b5` |
| 링크 색이 이상함 | `git revert --no-edit 067a0f5` |
| 대시보드 헤더가 이상함 | `git revert --no-edit 5a707bb 4ca24dd` |
| 메뉴 높이·그림자가 이상함 | `git revert --no-edit 198c905` |
| 이번 작업 전부(5개) | `git revert --no-edit 5a707bb 198c905 4ca24dd 067a0f5 68c23b5` |

---

# 2026-08-12 · 전체 통일 마무리 (카드 모서리·버튼·제목·아바타)

**상태: [실제 반영됨]** — 2026-08-12 실서비스 배포 완료. www.corezent.com 에서 확인 가능
**미리보기**: https://corezent-saas-git-preview-ui-final-20260812-core-zent.vercel.app (Vercel 로그인 필요)

## 무엇을 고쳤나요

1. **카드 모서리 12px 통일**(확정안) — 대시보드와 관리자를 오가도 같은 사이트로 느껴집니다.
   값은 한 곳(디자인 토큰)에서 정해집니다. 확인 창(모달)은 원래대로 더 둥급니다.
2. **저장·제출 버튼이 휴대폰에서 전부 가로로 꽉 찹니다**(넓은 화면은 내용만큼). 누르는
   높이도 44px로 통일했습니다.
3. **카드 제목 아래 구분선 제거**(확정안) — 고객지원·설정 등 9곳이 같은 모양이 됐습니다.
4. **제휴 화면 제목이 두 단계**가 됐습니다 — 카드 밖 회색 대문자(큰 묶음), 카드 안
   진한 글씨(개별 카드).
5. **회원 아바타 색이 이름에 따라 자동**(확정안) — 초록·노랑·파랑 3색이 섞여 나오고,
   같은 사람은 언제나 같은 색입니다. 탈퇴 회원만 빨간색(상태 표시), 이름 없으면 회색 ?.

## 확인 목록 — 위험한 순서대로

### 1. 관리자 여러 화면 훑기 (35개 파일이 바뀐 라운드)

- /admin 개요·사용자·주문·매출·설정·콘텐츠 관리를 차례로 열기
- ✅ **정상**: 카드 모서리가 대시보드와 같은 정도로 둥글고, 화면이 평소처럼 뜨면 통과

### 2. 관리자 → 사용자 관리

- 아바타 색이 회원마다 다르게(3색) 나오는지, **탈퇴 회원만 빨간색**인지
- 새로고침해도 같은 사람이 같은 색인지

### 3. 휴대폰(또는 창 좁게)에서 저장 버튼

- 관리자 설정·제품 편집·대시보드 설정에서 저장 버튼이 **가로로 꽉 차는지**
- 저장·취소가 나란한 곳(제품 편집 등)은 저장이 넓고 취소가 옆에 붙는지

### 4. 대시보드 → 제휴

- "추천 성과"·"적립 현황"(회색 대문자)이 큰 묶음, "내 추천 링크"·"정산 계좌"(진한
  글씨)가 카드 제목으로 구분돼 보이는지

### 5. 카드 제목 구분선

- 고객지원(대시보드)·설정(관리자)에서 제목 아래 줄이 없어도 제목과 내용이 답답하지
  않게 떨어져 보이는지

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 카드 모서리가 이상함 | `git revert --no-edit 406142e f0b96b6` |
| 버튼이 이상함 | `git revert --no-edit 406142e f9701ef` |
| 카드 제목·구분선이 이상함 | `git revert --no-edit 0ea4b66` |
| 제휴 제목이 이상함 | `git revert --no-edit 459cd1c` |
| 아바타가 이상함 | `git revert --no-edit 406142e 2383ed7` |
| 이번 작업 전부(6개) | `git revert --no-edit 406142e 2383ed7 459cd1c 0ea4b66 f9701ef f0b96b6` |

---

# 2026-08-12 (5차) — 사이트 문구를 지니워크 기준으로 교체

## 무엇이 바뀌었나

1. **홈 문구 전체가 지니워크 얘기로** — 상단 배너, 큰 제목(히어로), 특징 3개,
   이용 방법(6단계→4단계), 맨 아래 권유 문구(CTA)가 전부 새 문구입니다.
2. **FAQ가 9개로** — 지니워크 질문 8개 + 환불 정책(맨 뒤). 기존 결제·구독 질문
   4개는 지운 게 아니라 감춰둔 것이라 언제든 되살릴 수 있습니다.
3. **푸터** — 소개문이 "공무원의 행정문서 작성을 돕는 프로그램을 만듭니다."로,
   개인정보보호책임자가 유승목으로, 물품분류번호 43232698이 새로 표시됩니다.
4. **검색 노출 문구(SEO)** — 브라우저 탭 제목과 검색 결과 설명이 지니워크
   기준으로 바뀌었고, 이제 홈도 관리자 → 설정의 SEO 문구를 그대로 따릅니다.
5. **기관 도입 페이지** — 조달청 등록 정보 아래에 우선구매 대상·보유 자격·판매
   채널 세 줄이 추가됐습니다. (메뉴에서는 숨겨져 있어 주소로 직접 들어가야 보입니다)
6. **안 바뀐 것** — 요금 페이지 전체, 상품 설명, 회사소개 통계, 고객 후기(계속 숨김).

미리보기: https://corezent-saas-git-preview-site-copy-20260812-core-zent.vercel.app

## 확인 목록 — 위험한 순서대로

### 1. 홈 (corezent.com)

- 상단 배너·큰 제목·특징 3개·이용 방법 **4단계**·FAQ·맨 아래 문구가 전부
  지니워크(공무원 행정문서) 얘기인지
- ✅ **정상**: 옛 문구("일 잘하는 사람들의 AI 소프트웨어" 등)가 어디에도 없으면 통과
- 홈 중간의 **요금 카드 구역은 전과 똑같아야** 합니다

### 2. 요금 페이지 (corezent.com/pricing)

- ✅ **정상**: 이번 작업에서 한 글자도 안 건드렸으므로 **전과 완전히 같아야** 합니다

### 3. 브라우저 탭 제목

- 홈을 열었을 때 탭에 "지니워크 — 공무원 행정문서 작성 AI 프로그램 | 코어젠트"

### 4. FAQ (corezent.com/faq)

- 질문이 9개, "어떤 프로그램인가요?"가 처음, "환불 정책"이 맨 뒤인지

### 5. 기관 도입 (corezent.com/public-sector — 주소로 직접)

- 조달청 등록 정보에 물품식별번호 26391406 · 물품분류번호 43232698 두 카드
- 그 아래 우선구매 대상 · 보유 자격 · 판매 채널 세 줄

### 6. 푸터 (아무 페이지 맨 아래)

- 소개문·개인정보보호책임자 유승목·물품분류번호 표시

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 홈·FAQ·배너 등 문구가 이상함 | `db-backup/2026-08-12_문구백업_지니워크교체전.sql` 전체를 Supabase SQL Editor에서 실행 |
| 푸터 소개문이 이상함 | `git revert --no-edit caa33d2` |
| 기관 도입 세 줄이 이상함 | `git revert --no-edit a8142f0` |
| 홈 탭 제목·검색 문구가 이상함 | `git revert --no-edit e6f1078` |
| 코드 전부(3개) | `git revert --no-edit e6f1078 a8142f0 caa33d2` (문구는 위 SQL로 별도 복원) |

---

# 2026-08-12 (6차) — 공개 페이지 정리 (요금 카드·구매 바·맨 위로·문구)

## 무엇이 바뀌었나

1. **요금 카드 기능 설명이 3줄까지** — 전에는 한 줄로 잘려 "문서 26종 + 무제한 생성: 공문·보고…"
   처럼 끝이 안 보였습니다. 홈 요금 구역과 요금 페이지 둘 다 적용, 문구 자체는 그대로.
2. **"맨 위로" 버튼이 오른쪽 아래로** — 화면 가운데 아래에 떠 있던 것을 오른쪽 아래로.
   구매 바 바로 위에 앉고, 휴대폰에서 누르기 쉽게 키웠습니다.
3. **쿠키 동의 창 겹침 해소** — 상품 상세에서 쿠키 창의 허용 버튼이 구매 바에
   가려 눌리지 않던 것을 구매 바 위로 올렸습니다.
4. **구매 바 라벨(기간·PC개수·수량·결제방법)이 커지고 진해짐** — 작고 흐려 안 보이던 것.
   ※ 부동산 계산기의 '계산 기준' 등 같은 부품을 쓰는 라벨도 함께 또렷해집니다.
5. **홈 요금 카드 버튼 줄 정렬** — 수량칸 없는 카드(지니워크)와 있는 카드의 버튼 높이가
   어긋나던 것. 버튼이 모두 카드 맨 아래에 붙습니다(기능 목록이 버튼 위로 이동).
6. **홈에서 "제품 둘러보기"로 건너뛸 때 제목이 메뉴에 안 가림**.
7. **옛 문구 2곳 교체** — 홈 제품 구역 제목 "공무원 업무를 위한 프로그램",
   히어로 아래 작은 배지 4개(계산은 코드가 · 자료는 내 PC에 · 한글·PDF 바로 출력 · 조달청 등록 제품).
8. **안 바뀐 것** — 가격·할인·수량 계산·결제 연결 전부, 히어로 본문 문구, 상품 설명 내용.
   제품 상세의 표 폭 맞추기(Wave 6)는 대표님이 넣으신 설명 안에서 폭이 정해져 있어
   깨질 위험 때문에 건너뛰었습니다(아래 참고).

미리보기: https://corezent-saas-git-preview-public-pages-20260812-core-zent.vercel.app

## 확인 목록 — 위험한 순서대로

### 1. 상품 상세 (corezent.com/product/geniework)

- 아래로 스크롤: 하단 구매 바에서 **기간·PC개수·수량을 직접 골라보기** —
  라벨이 또렷한지, 고른 값대로 가격이 바뀌는지
- ★ **"구매하기" 버튼은 누르지 마세요** (실제 결제로 이어집니다)
- "맨 위로" 버튼이 **오른쪽 아래**, 구매 바와 안 겹치는지
- 시크릿 창으로 열면 쿠키 창이 뜨는데, **허용 버튼이 눌리는지**

### 2. 요금 페이지 (corezent.com/pricing)

- 카드의 기능 설명이 끝까지(최대 3줄) 보이는지, 카드끼리 높이가 맞는지
- 가격 숫자가 전과 같은지 (이번에 가격은 안 건드렸습니다)

### 3. 홈 (corezent.com)

- 요금 구역: 세 카드의 **버튼이 같은 줄**인지, 기능 설명 3줄
- 제품 구역 제목 "공무원 업무를 위한 프로그램", 히어로 아래 배지 4개 새 문구
- 히어로 큰 제목·부제는 **대표님이 고치신 그대로**여야 합니다
- "제품 둘러보기" 클릭 → 제품 구역 제목이 메뉴에 안 가리고 보이는지

### 4. 휴대폰(또는 창 좁게)

- 상품 상세 구매 바가 두 줄로 접히고 가로 스크롤이 없는지
- "맨 위로" 버튼이 본문을 크게 가리지 않는지

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 요금 카드 설명·높이가 이상함 | `git revert --no-edit 5263624 b909ce0` |
| 맨 위로·쿠키 창이 이상함 | `git revert --no-edit 5263624 51d5378` |
| 구매 바 라벨이 이상함 | `git revert --no-edit 78f714c` |
| 홈 카드 버튼 줄이 이상함 | `git revert --no-edit e5b4fdf` |
| 건너뛰기 위치가 이상함 | `git revert --no-edit 747ce53` |
| 문구 2곳이 이상함 | `git revert --no-edit f7fdcb3` |
| 이번 작업 전부(7개) | `git revert --no-edit 5263624 f7fdcb3 747ce53 e5b4fdf 78f714c 51d5378 b909ce0` |

※ 같은 시간대에 세금 세션 커밋 2개(84dfb56·c9b4d76 — 부동산 계산기 허브)가 함께
배포됐습니다. 위 명령들은 세금 쪽을 건드리지 않습니다.

---

# 2026-08-12 (7차) — 관리자 로그 정리 + 편집 미리보기 버튼 + 금액 색

## 무엇이 바뀌었나

1. **모니터링 로그가 쓸 수 있는 화면으로** (관리자 → 모니터링 로그)
   - 종류(이메일/웹훅)·상태(성공/실패)·기간(24시간/7일/30일/전체) 필터와 검색,
     20건씩 페이지 나누기가 생겼습니다.
   - **필터 기본값은 전부 "전체"** — 실패 기록이 처음 화면에서 가려지는 일이 없습니다.
   - 같은 오류는 위쪽에 "○○ 오류 · 39회 (마지막 …)"로 묶여 보이고, 펼치면
     쉬운 한글 설명 + 영어 원문 전문 + 개별 건 전부가 나옵니다. 기록 자체는
     하나도 지우거나 바꾸지 않았습니다(보여주는 방식만 변경).
   - 시각에 연도가 붙었습니다.
2. **콘텐츠 편집마다 "새 탭에서 보기" 버튼** — 히어로·CTA·배너는 "저장 후 새 탭에서
   보기"(저장이 실패하면 탭을 열지 않음), FAQ·특징·이용 방법·후기·섹션·소개는
   바로 보기. 실시간 미리보기 패널은 만들지 않았습니다(대표님 결정).
3. **주문 화면 총 매출 숫자가 초록 → 검정** — 금액 기본색 규칙. 할인 차감(초록)·
   검토 필요(빨강)·제휴 상태색은 뜻이 있어 그대로 뒀습니다.

미리보기: https://corezent-saas-git-preview-admin-logs-20260812-core-zent.vercel.app

## 확인 목록 — 위험한 순서대로

### 1. 관리자 → 모니터링 로그

- 처음 화면에 **실패 40건이 그대로 보이는지**(가려지면 문제)
- 위쪽 묶음에서 "환불 처리 실패 · 39회"를 펼쳐 한글 설명·영어 원문·개별 건이 다 보이는지
- 필터·검색·페이지 이동이 되는지, 시각에 연도(2026년)가 있는지

### 2. 관리자 → 콘텐츠 → 히어로

- 아무 글자나 살짝 바꾸고 "저장 후 새 탭에서 보기" 클릭 → 저장되고 홈이 새 탭으로
  열리며 바뀐 글이 보이는지 (확인 후 원래대로 되돌려 다시 저장해 주세요)
- ✅ **정상**: 경고창 없이 새 탭이 열림. "막혔다" 경고가 계속 뜨면 문제

### 3. 관리자 → 콘텐츠 → FAQ

- 오른쪽 위 "새 탭에서 보기" → FAQ 페이지가 새 탭으로 열리는지

### 4. 관리자 → 주문

- 위쪽 총 매출 숫자가 **검정**인지 (다른 기능·CSV 내려받기 그대로인지)

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 로그 화면이 이상함 | `git revert --no-edit 25d931c 81028dc 7e3c802` |
| 편집 보기 버튼이 이상함 | `git revert --no-edit 25d931c ec4da65` |
| 총 매출 색이 이상함 | `git revert --no-edit d8396d6` |
| 이번 작업 전부(5개) | `git revert --no-edit 25d931c d8396d6 ec4da65 81028dc 7e3c802` |

※ 세금 세션 커밋 2개(0c90442 룰 저장 검증·0e90a1a 인계서)가 함께 배포됐습니다.
위 명령들은 세금 쪽을 건드리지 않습니다.

---

# 2026-08-13 — 편집기 영어 예비 문구 제거 + 환영 메일 제목

## 무엇이 바뀌었나

1. **관리자 콘텐츠 편집기의 "영어 예비 문구" 위험 제거** (히어로 · CTA · 공지 배너)
   - 예전에는 DB에서 문구 키가 지워지면 편집기에 **영어 문구**가 미리 채워졌고,
     그 상태로 저장 한 번이면 홈 화면이 영어로 덮일 수 있었습니다.
   - 이제 편집기와 홈 화면이 **같은 한국어 예비 문구 한 벌**을 함께 씁니다.
     칸을 비웠을 때 흐리게 보이는 안내 문구도 옛 영어·옛 GeniePost 달러 가격에서
     현재 한국어 문구로 바뀌었습니다.
   - 대표님이 넣어두신 **실제 문구(DB)는 하나도 바뀌지 않았습니다.**
2. **가입 환영 메일의 받은편지함 제목** — "CoreZent 가입을 환영합니다" →
   "**코어젠트 가입을 환영합니다**" (본문·버튼은 어제 정리한 그대로)
3. **계좌이체 라이선스 자동 발급은 만들지 않았습니다** — 발급 코드가 결제(웹훅)
   코드 안에 있어 떼어내야 하는데, 대표님 지시로 취소했습니다. 주문 목록의
   [결제 확인] 버튼은 지금처럼 상태만 바꾸고, 라이선스는 대표님이 직접 처리하시는
   방식이 유지됩니다.

미리보기: https://corezent-saas-git-preview-copy-defaults-20260813-core-zent.vercel.app

## 확인 목록 — 위험한 순서대로

### 1. 홈 화면 (달라 보이면 안 됨)

- https://www.corezent.com 첫 화면 — 히어로 문구·상단 공지 배너·맨 아래 CTA가
  평소와 똑같은지
- ✅ **정상**: 아무 변화 없음. 영어 문구가 보이면 문제

### 2. 관리자 → 콘텐츠 → 히어로 / CTA / 공지 배너

- 각 칸에 지금 쓰시는 한국어 문구가 그대로 들어있는지
- 아무 칸이나 글자를 지워봤을 때 흐리게 보이는 안내 문구가 **한국어**인지
  (확인만 하고 저장하지 말고 원래대로 되돌려 주세요)

### 3. 가입 환영 메일 (새 가입이 생겼을 때)

- 받은편지함 제목이 "코어젠트 가입을 환영합니다"인지

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 홈·편집기 예비 문구가 이상함 | `git revert --no-edit 056f9d9 c617e82` |
| 메일 제목이 이상함 | `git revert --no-edit dc7a3a8` |
| 이번 작업 전부(3개) | `git revert --no-edit 056f9d9 dc7a3a8 c617e82` |

※ 세금 세션 커밋 1개(b9dac85 중개수수료 계산기)가 함께 배포됩니다.
위 명령들은 세금 쪽을 건드리지 않습니다.

---

# 2026-08-13 (2차) — 관리자 알림 (새 주문 · 입금 대기 · 새 문의)

## 무엇이 바뀌었나

1. **새 주문이 들어오면 메일이 옵니다** — 카드·계좌이체 모두.
   - 받는 주소는 관리자 → 설정 → 일반의 **고객지원 이메일**입니다(코드에 박지 않음).
   - 계좌이체 주문 메일은 제목부터 다릅니다: "새 계좌이체 주문 #번호 — 입금 확인·
     라이선스 수동 발급 필요". 카드 주문은 "새 주문 #번호 (카드)".
   - 메일에는 주문 번호·구매자·상품·수량·금액·결제 방법·상태가 담깁니다.
     **라이선스 키·카드 정보는 절대 안 담습니다.**
   - 주문 여러 건이 와도 **한 건당 한 통씩** 옵니다(묶지 않음).
2. **새 고객지원 티켓이 생기면 메일이 옵니다** — 계정·제목·내용 앞 80자만.
   같은 계정이 30분 안에 여러 번 내면 첫 통만 옵니다(폭주 방지 — 나머지는
   관리자 → 고객지원 목록에 전부 남음).
3. **새 문의(일반·기관 견적) 알림은 원래부터 있었고 그대로입니다.** 두 통이 가지
   않게 확인 완료. 스팸(봇)은 알림이 가지 않습니다.
4. **설정에서 켜고 끌 수 있습니다** — 관리자 → 설정 → "관리자 알림" 카드.
   새 주문·새 티켓 각각. **기본은 모두 켜짐**입니다.
5. **알림이 실패해도 주문·문의 접수는 절대 실패하지 않습니다.** 메일은 응답이
   나간 뒤에 따로 발송되고, 실패하면 모니터링 로그에 남습니다. 지원 이메일이
   비어 있으면 알림을 건너뛰고 그 사실도 로그에 남습니다.
6. 계좌이체 알림의 **받는 주소가 코드에 박혀 있던 것을 설정 읽기로 통일**했습니다.

미리보기: https://corezent-saas-git-preview-admin-notify-20260813-core-zent.vercel.app

## 확인 목록

### 1. 관리자 → 설정
- "관리자 알림" 카드가 보이고, 두 스위치가 **켜짐**인지
- 일반 설정의 **고객지원 이메일**이 실제 받으실 주소로 채워져 있는지 (비어 있으면 알림이 안 갑니다!)

### 2. 알림은 실제 사건이 있어야 확인됩니다
- 가장 쉬운 확인: 대시보드 → 고객지원에서 **테스트 티켓을 하나** 내 보세요.
  잠시 후 지원 이메일로 "새 고객지원 티켓 — (대표님 계정)" 메일이 오면 정상.
- 다음 계좌이체·카드 주문이 들어올 때 메일이 오는지 보세요.
- 메일이 안 오면: 관리자 → 모니터링 로그에서 "관리자 알림 미발송" 또는 발송 실패
  기록을 확인하세요.

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 알림 전부 이상함 | 설정 → 관리자 알림에서 스위치를 끄면 즉시 멈춤 (코드 되돌리기 불필요) |
| 코드째 되돌리기 | `git revert --no-edit 078565c 6f310fe 4dcda9b 19adcde ce29d10` |

※ 세금 세션 커밋 3개(c18bccf·8885a7c·48ee8f1, 중개수수료 계산기)가 함께 배포됩니다.
위 명령은 세금 쪽을 건드리지 않습니다.

---

# 2026-08-13 (3차) — 기관 견적: 요청 접수 → 관리자 목록 → 견적서 PDF 발급

## 무엇이 바뀌었나

1. **기관 도입 페이지의 견적 요청이 제대로 된 접수가 됐습니다.**
   - 사업자등록번호·필요 시기 칸이 추가됐고, **PC 수는 최소 10대**(미만이면 개인
     구매 안내)입니다. 접수되면 지원 이메일로 알림 메일이 옵니다.
   - 이제 문의함이 아니라 **전용 저장소**에 쌓입니다.
2. **관리자 → 견적 요청** 메뉴가 생겼습니다. 접수일·기관명·담당자·PC 수·상태
   (접수됨/견적 발급됨)가 보이고, 행을 누르면 전체 내용이 펼쳐집니다.
3. **[견적서 만들기 (PDF)]** — 펼친 화면에서 상품 옵션과 수량을 고르면 도장이
   찍힌 견적서 PDF가 바로 내려받아집니다.
   - 금액은 **사이트 판매가(VAT 포함) 그대로**입니다: 합계 = 옵션 단가 × 수량.
     공급가액·부가세는 그 합계에서 역산해 표기합니다(관공서 서식).
   - **수량 주의**: 옵션 이름에 대수(예: 10PC용)가 이미 있으면 수량은 1로 두세요.
     30대 = "10PC용" × 수량 3. 화면에도 같은 안내가 있습니다.
   - 유효기간은 발행일로부터 30일, 견적 번호(CZQ-연도-0001)는 자동으로 겹치지 않게
     매겨지고, 같은 요청에 다시 발급하면 새 번호가 나옵니다.
   - **회사 정보(설정 → 견적서 공급자 정보) 중 하나라도 비면 발급이 막히고**
     무엇이 비었는지 알려줍니다. 빈칸 있는 견적서는 나가지 않습니다.
4. **주문 상세에 "기관 구매 정보" 칸**(기관명·사업자번호·담당자·세금계산서 번호)이
   생겼습니다. 전부 선택 입력 — 개인 주문은 비워 두면 됩니다.

미리보기: https://corezent-saas-git-preview-quotation-20260813-core-zent.vercel.app

## ✍️ 대표님이 하셔야 할 것 (순서대로)

1. **Supabase SQL Editor에서 파일 2개 적용** (적용 전에는 접수·기관 정보 칸이 동작하지 않습니다):
   - `supabase/migrations/060_quote_requests.sql` (견적 저장소)
   - `supabase/migrations/061_orders_org_info.sql` (주문 기관 칸)
2. **관리자 → 설정 → 견적서 공급자 정보** 확인 — 상호·사업자번호·대표자·주소·
   업태·종목·전화 7칸을 미리 채워 두었습니다. 값이 맞는지 한번 봐 주세요.
3. **견적서 시험 발급**: 기관 도입 페이지에서 시험 요청을 하나 넣고 →
   관리자 → 견적 요청 → 행을 펼쳐 → 옵션·수량 선택 → [견적서 만들기] →
   내려받은 PDF에서 **한글·도장·금액**을 확인하세요.

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 견적 관련 전부 | `git revert --no-edit 81c91d8 f3b4bf1 ae3f7b5 c0210d5 cfec829 e9df257` |
| 주문 기관 칸만 | `git revert --no-edit ae3f7b5` |
| PDF 발급만 | `git revert --no-edit 81c91d8 c0210d5` (요청 접수·목록은 유지됨) |

※ 세금 세션 커밋 여러 개(양도소득세 S4 계열)가 함께 배포됩니다. 위 명령은 세금 쪽을 건드리지 않습니다.

---

# 2026-08-14 — 손님 셀프 서비스: 결제수단 변경 · 이메일 변경

## 무엇이 바뀌었나

1. **대시보드 → 결제에 [결제수단 변경] 버튼이 생겼습니다.**
   - 구독이 연결된 줄에만 보입니다. 구독이 없는 손님에게는 안 보입니다.
   - 누르면 결제사(Lemon Squeezy)의 **결제수단 변경 전용 화면**으로 이동합니다.
     카드 정보는 우리 서버를 거치지 않고 결제사에서만 다룹니다.
   - 일부러 **결제수단 변경만 되는 화면**으로 보냅니다. 결제사의 전체 관리
     화면에서는 요금제 변경까지 가능한데, 그건 아직 우리 쪽이 따라가지 못해
     (플랜 올리기 별도 라운드) 화면·라이선스가 어긋날 수 있기 때문입니다.
   - 이동 주소는 보안상 **24시간만 유효**해서, 누를 때마다 결제사에서 새로
     받아옵니다. 실패하면 한국어로 알려줍니다.
   - 결제 실패(카드 만료 등)로 상태가 "만료"로 보이는 구독에도 버튼이
     보입니다 — 결제수단을 바꿔야 할 손님이 바로 그 사람이기 때문입니다.

2. **대시보드 → 설정에 "이메일 변경" 칸이 생겼습니다.**
   - 새 주소를 넣으면 **확인 메일**이 가고, 메일의 링크를 눌러야 실제로
     바뀝니다. 확인 없이 바로 바뀌는 일은 없습니다(오타 보호).
   - 보안 설정에 따라 **지금 주소로도 확인 메일**이 갑니다 — 누가 몰래
     바꾸려 하면 원래 주소에서 알아챌 수 있습니다.
   - 이미 다른 계정이 쓰는 주소면 "바꿀 수 없다"고 알려줍니다.
   - 확인 링크는 **변경을 요청한 그 브라우저에서** 열어야 합니다(화면에도
     같은 안내가 있습니다). 휴대폰 메일앱 등 다른 기기에서 열면 실패할 수
     있습니다.
   - **바뀌지 않는 것**(정상입니다): 결제사에 등록된 주소(영수증은 계속 옛
     주소로 감 — 화면에 안내 있음), 이미 발급된 라이선스 기록·시트의 이메일,
     과거 주문의 입금자 이메일. 전부 기록용이고 로그인·프로그램 인증과는
     무관합니다. 주문·라이선스 내역은 끊기지 않습니다.

미리보기: https://corezent-saas-git-preview-account-self-service-core-zent.vercel.app

## 확인 목록

### 1. 설정 → 이메일 변경 (지금 바로 확인 가능)
- 대시보드 → 설정에 "이메일 변경" 칸이 보이는지
- 시험용 주소로 바꿔 보기: 새 주소 입력 → [확인 메일 보내기] → 메일이
  오는지 → 링크 클릭 → 설정 화면으로 돌아오고 현재 이메일이 바뀌는지
- 이미 쓰는 주소(예: 대표님 다른 계정)를 넣으면 거절 안내가 나오는지

### 2. 결제 → 결제수단 변경 (실제 구독이 있어야 확인됩니다)
- **지금은 카드 구독 주문이 0건이라 버튼이 보일 줄이 없습니다.** 다음 실제
  구독 결제가 들어온 뒤, 그 손님(또는 시험 결제) 계정의 결제 화면에서
  [결제수단 변경]이 보이는지 → 눌러서 결제사 화면이 열리는지 확인하세요.
- 구독 없는 계정(계좌이체·단건 구매)에는 버튼이 안 보이는 게 정상입니다.

### 3. 운영 설정 확인 (Supabase 대시보드 — 한 번만)
- Authentication → 설정에서 **Secure email change**가 켜져 있는지
  (켜져 있으면 옛 주소·새 주소 양쪽 확인 — 권장). 꺼져 있으면 새 주소
  확인만으로 바뀝니다.
- Authentication → URL Configuration의 Redirect URLs에
  `https://www.corezent.com/auth/callback` 계열이 허용돼 있는지
  (회원가입 인증이 이미 같은 주소를 쓰고 있어 보통 이미 돼 있습니다).

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 이번 작업 전부(3개) | `git revert --no-edit d5f9c47 d5b83f1 7e653e1` |
| 일부만 되돌리고 싶을 때 | 마지막 수정 커밋(d5f9c47)이 두 기능에 걸쳐 있어 개별 되돌리기는 충돌할 수 있습니다 — 요청해 주시면 정리해서 되돌립니다 |

※ 이번 배포(main)에는 세금 세션 커밋이 **함께 올라가지 않았습니다**(제 커밋 3개만).
다만 오늘 아침 세금 세션의 인계서 문서 커밋 1개(48061d3)를 제가 먼저 푸시했습니다
— 문서만이라 화면 영향은 없습니다.

---

# 2026-08-14 (2차) — 고객 문의 첨부·유형 · 사용자 목록 정비 · 재색인

## 무엇이 바뀌었나

1. **로그인 고객의 문의 창에 첨부와 문의 유형이 생겼습니다.**
   - 첨부는 비회원 문의와 같은 끌어다 놓기 칸, 같은 5MB 제한입니다(부품을 나눠 씀).
   - 파일은 **비공개 저장소**에만 저장됩니다 — 주소만 알아도 열리지 않고, 관리자
     화면에서만 기한 있는(1시간) 내려받기 주소가 만들어집니다.
   - **실행 파일은 첨부할 수 없습니다**(이미지·PDF·문서·압축만 허용). 비회원
     문의 폼에는 원래 형식 제한이 없었고, 그쪽 동작은 바꾸지 않았습니다.
   - 문의 유형 6가지: 설치 실패 / 키 인증 / PC 변경 / 증빙 요청 / AI 키 오류 / 기타.
     관리자 → 고객지원 목록·상세에 유형이 보입니다.
   - 접수가 실패하면 이제 화면에 실패라고 나옵니다(전에는 조용히 사라졌습니다).
2. **관리자 → 사용자 관리가 수백 명이 돼도 버티는 구조가 됐습니다.**
   - 검색(전체 회원 대상, 엔터로 검색)·정렬(가입일/이름)·페이지 나누기.
   - **[CSV 내보내기]**: 지금 화면의 검색 조건 그대로 내보냅니다. 확인 창을
     거치고, **누가 언제 내보냈는지 기록이 먼저 남아야만** 파일이 나갑니다.
     엑셀에서 한글이 깨지지 않게 처리했고, 칸은 이름·이메일·역할·상태·가입일
     5개뿐입니다(비밀번호·계좌 등은 절대 안 들어감).
3. **검색엔진 재색인** — 사이트 제목·설명은 어제 넣으신 지니워크 문구가 이미
   정본으로 들어가 있음을 확인했습니다(새로 쓰지 않음). 제출을 1회 실행했으나
   **두 채널 모두 실패** — 아래 "대표님이 하셔야 할 것" 참조.

미리보기: https://corezent-saas-git-preview-attachment-userlist-core-zent.vercel.app

## ✍️ 대표님이 하셔야 할 것 (순서대로)

1. **Supabase SQL Editor에서 `supabase/migrations/062_support_attachments_category.sql` 적용**
   — 적용 전에는 문의 유형이 저장되지 않고, 첨부는 "업로드 실패" 안내가 나갑니다
   (접수 자체는 정상). 적용 후 파일 하단의 확인 쿼리도 실행해 보세요.
2. **구글 재색인 켜기**: 구글 클라우드 콘솔에서 서비스 계정 프로젝트에
   **Web Search Indexing API 사용 설정** + 서치 콘솔(Search Console)에 그 서비스
   계정을 **소유자로 추가**. 그 뒤 관리자 → 설정 → "검색엔진 색인 재요청" 버튼을
   한 번 누르면 됩니다. (빙·네이버 쪽도 이번엔 422로 거절 — 위 조치 후 같은
   버튼으로 함께 재시도되니 결과를 보시면 됩니다.)
   ★ 재색인은 요청해도 검색 결과에 바로 안 보입니다 — 보통 며칠, 길면 몇 주 걸립니다.

## 확인 목록

- 대시보드 → 고객지원: 새 문의 폼에 유형 선택과 첨부 칸이 보이는지, 시험 문의
  1건을 첨부(스크린샷 등)와 함께 접수해 보고 → 관리자 → 고객지원 상세에서 유형과
  📎 첨부 내려받기가 되는지 (062 적용 후에만 정상)
- 관리자 → 사용자 관리: 검색·정렬·페이지가 동작하는지, [CSV 내보내기] → 확인 창 →
  받은 파일을 엑셀에서 열어 한글이 멀쩡한지
- 비회원 문의(/contact)는 **그대로**여야 정상입니다(모양·5MB 문구 동일)

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 이번 작업 전부(3개) | `git revert --no-edit 65f19c4 35e3277 c9fc8fc` |
| 사용자 관리만 | `git revert --no-edit 35e3277` (단, 65f19c4가 이 화면도 고쳐 충돌 가능 — 요청해 주시면 정리해서 되돌립니다) |

※ **이번 배포에 세금 세션 커밋 4개가 함께 올라갔습니다**(6274b5a·c4a3cd2·0e9f059·e88fe9e —
재산세·종합부동산세 S5, 전부 '준비 중'이라 화면에는 안 보입니다). 위 명령은 세금 쪽을
건드리지 않습니다.

---

# 2026-08-14 (3차) — 플랜 올리기 (1PC → 3PC 등)

## 무엇이 바뀌었나

1. **결제사에서 플랜이 바뀌면 우리 라이선스가 따라 바뀝니다.**
   - 지금까지는 결제사가 "플랜 바뀌었다"고 알려줘도 읽지 않아, 돈만 바뀌고
     사용 PC 대수는 그대로였습니다. 이제 통지가 오면 사용 PC 대수(전용
     라이선스 DB)와 결제 화면의 옵션 표시(본체 DB)가 **함께** 바뀝니다.
   - 반영에 실패하면 관리자 → 모니터링 로그에 사유가 남고, 결제사가 통지를
     다시 보내 자동으로 재시도됩니다.
   - **줄이는 통지가 와도**(결제사 쪽에서 손님이 스스로 낮춘 경우) 등록된
     PC는 빼지 않습니다 — 쓰던 PC는 계속 인증되고, 새 PC 등록만 새 한도로
     막힙니다.
2. **대시보드 → 결제에 [플랜 올리기] 버튼이 생겼습니다.**
   - 활성 구독이면서 더 큰 옵션이 있을 때만 보입니다(구독 없음·최대 플랜이면
     안 보임). **올리기만** 됩니다 — 낮추기 없음.
   - **금액은 화면에 표시하지 않습니다** — 결제사에 "바꾸기 전 얼마인지"를
     알려주는 기능이 없어, 차액은 결제사가 정산해 청구서로 안내된다고
     문구로 알립니다(우리가 금액을 계산하는 코드는 0줄입니다).
   - 요청하면 결제사가 처리하고, 우리 화면·라이선스는 통지를 받은 뒤
     바뀝니다(화면에도 같은 안내가 있습니다).
3. **결제사 관리 화면 제약(결제수단 전용)은 그대로 뒀습니다** — 실제 구독으로
   이번 기능이 검증되기 전까지는 지금 상태가 안전하다고 판단했습니다.

미리보기: https://corezent-saas-git-preview-plan-upgrade-core-zent.vercel.app

## 확인 목록

- **실제 구독이 있어야 확인됩니다.** 지금 카드 구독 주문이 0건입니다.
  다음 실제 구독(또는 시험 결제)이 생기면: 그 계정의 대시보드 → 결제에서
  [플랜 올리기] → 상위 옵션 선택 → 변경 요청 → 잠시 후 새로고침해서
  옵션 표시가 바뀌었는지, 앱에서 새 PC 대수로 등록되는지 확인하세요.
- 지금 바로 볼 수 있는 것: 결제 화면이 전과 똑같이 뜨는지(구독 0건이라
  버튼이 안 보이는 게 정상), 관리자 → 모니터링 로그가 정상인지.
- ★ 옵션 행 관리 유의: 관리자 → 상품의 옵션 행에서 **license_tier가 비었거나
  목록(1pc/3pc/5pc/10pc/lite/pro/max) 밖이면 그 옵션은 플랜 올리기 후보에서
  자동 제외**됩니다. 새 옵션을 만들 때 tier를 꼭 채워 주세요.

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 이번 작업 전부(3개) | `git revert --no-edit ccd5c96 ae80e14 2796352` |
| 화면(버튼)만 | `git revert --no-edit ae80e14` (웹훅 반영은 유지 — 결제사 쪽 변경은 계속 따라감) |

※ 세금 세션 커밋 여러 개(S5 재산세·종합부동산세 계열)가 같은 기간에 함께
배포됐습니다. 위 명령은 세금 쪽을 건드리지 않습니다.

---

# 2026-08-14 (4차) — 매뉴얼: AI 키 발급 안내를 지니워크 기준으로 교체

## 무엇이 바뀌었나

1. **매뉴얼(상단 메뉴 "매뉴얼" → /docs)의 AI 키 발급 안내가 지니워크 기준으로
   바뀌었습니다.** 대표님이 확인하신 문구 그대로이고(대조 프로그램으로 104줄
   전체 일치 확인 — 한 글자도 안 바꿈), 순서는 시작하기 전에 → Claude → Gemini
   3편입니다. ⚠️·💡·✅ 기호는 색 있는 알림 상자로 바뀌어 표시됩니다.
2. **옛 지니포스트용 ChatGPT·DeepSeek 문서는 숨겼습니다** — 사이드바·매뉴얼
   검색창·검색엔진 제출 목록에서 전부 빠집니다. 파일은 지우지 않아 문서 머리의
   표식 한 줄만 지우면 되살아납니다. (주소를 직접 치면 아직 열립니다 — 지시하신
   범위대로 사이드바·검색·사이트맵만 뺐습니다.)
3. 매뉴얼 첫 화면의 낡은 소개("4종…")도 지정하신 문장으로 바뀌었습니다.

미리보기: https://corezent-saas-git-preview-manual-api-keys-core-zent.vercel.app

## 확인 목록

- https://www.corezent.com/docs — 첫 화면 소개가 "지니워크를 쓰려면 Claude와
  Gemini 키가 모두 필요합니다…"인지
- 사이드바 "AI API 키 발급"에 3편만 보이는지(시작하기 전에·1. Claude·2. Gemini)
- 각 문서에서 표·알림 상자·굵은 글씨가 제대로 보이고, 링크가 새 창으로 열리는지
- 매뉴얼 상단 검색창에 "ChatGPT" 검색 시 안 나오는지
- ★ 남은 것 하나: 첫 화면 아래 "GeniePost Max 매뉴얼" 구절(구글 사이트 링크)은
  지시가 없어 그대로 뒀습니다 — 문구를 주시면 반영합니다.

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 이번 작업 전부(2개) | `git revert --no-edit b551101 9d817c3` |
| ChatGPT·DeepSeek 다시 보이게 | 두 파일 머리의 `hidden: true` 줄 삭제 + meta.json pages에 이름 추가 (요청하시면 처리) |

※ **이번 배포에 세금 세션 커밋 4개가 함께 올라갔습니다**(ca4a78a·a0dbb42·448db13·d04b103
— 등기비용·매도 실수령액 S6, 전부 '준비 중'이라 화면에는 안 보입니다). 위 명령은
세금 쪽을 건드리지 않습니다.

---

# 2026-08-14 — 관리자 작업 기록(감사 로그) 전면 확장 + "작업 기록" 화면 신설

## 무엇이 바뀌었나

1. **관리자 화면에서 무언가를 바꾸는 동작 전부가 이제 기록에 남습니다** — 제품
   만들기/고치기/순서 바꾸기, 변경 이력(체인지로그), 견적서 발급, 입금 확인, 기관
   정보, 사이트 설정, 랜딩 섹션, 콘텐츠 8개 화면(히어로·CTA·공지 배너·소개·고객
   후기·이용 방법·특징·FAQ), 제휴 관리, 고객지원 티켓 상태 변경.
2. 값이 바뀌는 동작은 **바꾸기 전 값과 바꾼 후 값이 함께** 남습니다. 긴 문구(제품
   설명·FAQ 답변 등)는 전문 대신 앞부분 80자와 글자 수만 남기고, 비밀값(계좌번호·
   예금주·메일 계정·비밀번호)은 값 없이 "바뀌었다"는 사실만 남습니다. 고객지원
   답변 본문은 남기지 않습니다(상태 변경만).
3. **관리자 → 시스템 → "작업 기록"** 메뉴가 새로 생겼습니다. 누가·언제·무엇을
   바꿨는지 표로 보이고, 누가/종류/기간 필터와 검색이 됩니다. 이 화면은 보기
   전용이라 기록을 고치거나 지울 방법이 없습니다.
4. 기록이 실패해도 원래 하던 저장·삭제는 그대로 됩니다(기록 때문에 일이 막히는
   경우 없음).

## 확인 목록

- https://www.corezent.com/admin/activity — 화면이 열리고 "작업 기록" 표가
  보이는지 (기록이 아직 없으면 "기록된 작업이 없습니다"가 정상)
- 관리자에서 아무 콘텐츠나 하나 고쳐 저장한 뒤 이 화면을 새로고침 —
  방금 한 일이 맨 위에 나타나는지 (전값 → 후값 포함)
- 관리자 → 설정에서 값 하나를 바꿔 저장 — 작업 기록에 남되, 계좌번호를 바꾼
  경우에는 번호가 아니라 "바뀌었다"는 표시만 남는지
- 기존 화면(제품·주문·고객지원·모니터링 로그)이 평소처럼 동작하는지

## 대표님 결정 대기 (이번에 일부러 안 한 것)

- **백업(Wave 3)** — 지시하신 대로 손대지 않았습니다. Supabase 플랜 확인 후
  지시 주시면 진행합니다.
- 작업 기록 테이블에 시각 정렬용 인덱스가 없어 기록이 수천 건 쌓이면 화면이
  느려질 수 있습니다 — DB 변경(마이그레이션)이라 승인 후 추가하겠습니다.
- 주문 기관 정보(담당자명·사업자번호)는 감사 목적상 전/후 원문을 남깁니다.
  가리기를 원하시면 말씀해 주세요.

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 이번 작업 전부 | `git revert --no-edit 46084dd` |

※ 이번 배포에는 다른 세션 커밋이 딸려 가지 않았습니다(제 커밋 1개만 반영).

---

# 2026-08-15 — 대표님 결정 3건 처리 (백업 안내 · 작업 기록 색인 SQL · 기관 정보 유지)

## 무엇이 바뀌었나

1. **백업** — 지시하신 대로 새 백업 기능은 만들지 않았습니다. 대신 복구 안내
   문서 **`Supabase_백업_복구_안내.md`** 를 만들었습니다(저장소 맨 위 폴더).
   화면 이름 따라가기 방식이고, 7일 지난 것은 못 되돌린다는 점을 명시했습니다.
   확인 결과 **업로드 파일(로고·문의 첨부 등)은 자동 백업에 포함되지 않습니다**
   — Supabase 공식 문서에 명시된 사실이며, 지키는 방법 3가지를 문서에 제시만
   해 뒀습니다(만들지 않음).
2. **작업 기록 색인** — SQL 파일
   **`supabase/migrations/063_admin_activity_log_created_index.sql`** 을
   만들었습니다. **실행은 하지 않았습니다** — 대표님이 하시는 방법:
   Supabase 접속 → **CoreZent 본체 프로젝트** → 왼쪽 메뉴 **SQL Editor** →
   파일 내용 전체를 붙여넣고 **Run** → 오류 없이 끝나면 성공입니다.
3. **주문 기관 정보** — 지시하신 대로 그대로 뒀습니다(코드 변경 없음).

## 확인 목록

- 위 방법으로 063 SQL 실행 후, https://www.corezent.com/admin/activity 가
  평소처럼 열리는지 (화면·데이터 변화 없음이 정상 — 속도만 대비되는 것)
- `Supabase_백업_복구_안내.md` 를 한번 읽어보시고, 이해 안 되는 표현이 있으면
  말씀해 주세요 — 고치겠습니다

## 문제가 생기면

이번 작업은 문서 2개와 SQL 파일 1개뿐이라 사이트 동작에는 아무 변화가 없습니다.
색인을 되돌리고 싶으면 SQL Editor에서:
`DROP INDEX IF EXISTS idx_admin_activity_log_created;`

---

# 2026-08-15 — 작업 기록(감사 로그) 유실 수정: 무엇을 바꿔도 기록이 남습니다

## 무엇이 바뀌었나

1. **원인** — 상품 수정 저장은 "바뀐 항목을 감지했을 때만" 기록했는데, 옵션 행의
   결제 주소·라이선스 대수 구분·표시 순서 같은 항목은 감지 목록에 없어 그것만
   바꾸면 기록이 안 남았습니다(어제 대표님이 겪으신 그 증상).
2. **수정** — 옵션 행의 모든 항목(금액·방식·주기·대수 구분·이름표·순서·결제
   주소·결제사 번호)이 바뀌면 전값→후값이 남습니다. 특히 **대수 구분(1PC↔10PC)**
   은 반드시 전/후가 남습니다.
3. **최소 기록** — 바뀐 게 하나도 감지되지 않아도 "누가 언제 저장했다"가 항상
   남습니다(내용에 "변경 감지 없음(저장만 실행됨)" 표시). 제품 수정·사이트 설정·
   히어로·공지 배너·CTA·소개 히어로 6곳 공통입니다.
4. 긴 문구는 앞 80자만, 비밀값(계좌·비밀번호 등)은 값 없이 사실만 — 기존 규칙
   그대로입니다. 기록이 실패해도 저장은 정상 진행됩니다.

## 확인 목록

- 관리자 → 제품 → 아무 제품이나 열어 **아무거나 하나** 바꿔 저장
  (가장 확실한 시험: 옵션 행의 결제 주소나 표시 순서만 바꾸기, 또는 아무것도
  안 바꾸고 저장 버튼만 누르기 — 예전엔 이 경우 기록이 안 남았습니다)
- https://www.corezent.com/admin/activity 새로고침 → 맨 위에 "제품 수정"이
  방금 시각으로 보이면 정상. 아무것도 안 바꿨다면 내용에 "변경 감지 없음"이 보임
- 히어로·공지 배너·CTA·소개·사이트 설정에서도 저장하면 같은 방식으로 남는지

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 이번 작업 전부 | `git revert --no-edit 5e98790` |

※ 이번 배포에는 세금 세션 커밋 2개(98d89ff·308edd1 — 종부세 개편안 W1·W2)가
함께 올라갑니다. 둘 다 "화면 미노출·현행 계산 불변" 단계라 손님 화면 변화는
없으며, 위 되돌리기 명령은 세금 쪽을 건드리지 않습니다.

---

# 2026-08-15 (2차) — 홈을 지니워크 전용으로

## 무엇이 바뀌었나

1. **홈 요금 섹션에 지니워크 카드 하나만** 나옵니다(월 9,900원부터 — 실제 판매
   가격을 그대로 읽으며 코드에 금액을 박지 않았습니다). 카드 아래 **「요금 보기」**
   버튼이 새로 생겨 요금 페이지로 이동합니다.
2. **관리자 → 설정 → 일반 설정**에 "홈 대표 제품" 입력칸이 생겼습니다. 비워 두면
   지니워크이고, 다른 제품 주소 이름(예: geniestock)을 넣으면 그 제품이 홈 대표가
   됩니다. 잘못된 이름을 넣거나 그 제품을 판매 중지하면 예전처럼 전체 제품이
   보입니다(홈이 비지 않게 하는 안전장치).
3. 제품 소개 섹션(지금 꺼져 있음)도 켜면 같은 규칙을 따릅니다.
4. **요금 페이지(/pricing)·제품 목록(/product)·기관 도입 페이지는 그대로**입니다
   — 이 설정은 홈에만 영향을 줍니다.

## 확인 목록

- https://www.corezent.com/ — 요금 섹션에 지니워크 카드 **하나만** 중앙에 보이고,
  아래에 「요금 보기」 버튼이 있는지. 버튼을 누르면 요금 페이지로 가는지
- https://www.corezent.com/pricing — **예전 그대로** 제품 3개가 보이는지
- https://www.corezent.com/product — **예전 그대로**인지
- 관리자 → 설정 → 일반 설정 → "홈 대표 제품"이 보이는지. 시험 삼아 geniestock을
  넣고 저장 → 홈 새로고침 → 지니스톡 카드로 바뀌는지 → 다시 비우고 저장 → 지니워크로
  돌아오는지 (이 저장도 작업 기록에 남습니다)

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 이번 작업 전부 | `git revert --no-edit 03d8dce` |
| 코드 되돌리기 없이 홈만 예전(3개)으로 | 관리자 → 설정 → "홈 대표 제품"에 `none` 처럼 없는 이름 입력 후 저장 |

---

# 2026-08-16 — 무료 체험 신청 버튼 + 방문→구매 측정

## 무엇이 바뀌었나

1. **「무료 체험 신청」 버튼**이 상단 메뉴(컴퓨터·휴대폰 모두)와 요금 페이지 카드
   아래에 생겼습니다. 누르면 구글 폼이 새 창으로 열립니다. 체험 기간·횟수는
   어디에도 적지 않았습니다(「신청하시면 안내드립니다」만).
2. 신청 주소는 **관리자 → 설정 → 일반 설정 → "무료 체험 신청 주소"**에서 바꿀 수
   있습니다. 비워 두고 저장하면 버튼이 사이트에서 숨겨집니다.
3. 방문→구매 흐름 측정이 붙었습니다: 체험 버튼 클릭·가입(이메일)·결제 시작·
   결제 완료 4가지. 개인정보는 담지 않습니다.

## 확인 목록

- 아무 페이지나 열어 상단 메뉴 오른쪽 「무료 체험 신청」(테두리 버튼)을 눌러
  **신청 폼이 실제로 새 창으로 열리는지** — 꼭 한번 눌러봐 주세요
- 휴대폰에서 메뉴(≡)를 열면 같은 버튼이 있는지
- 요금 페이지(corezent.com/pricing) 카드 아래에도 버튼+「신청하시면 안내드립니다」
- 측정 수치는 **바로 안 보입니다** — 구글 애널리틱스(analytics.google.com) →
  보고서 → 참여도 → 이벤트에서 보통 하루 뒤부터 trial_apply_click·sign_up·
  begin_checkout·purchase 이름으로 잡힙니다

## 문제가 생기면

버튼만 급히 숨기려면: 관리자 → 설정 → "무료 체험 신청 주소"를 비우고 저장(1분 내 반영).
코드 되돌리기: `git revert --no-edit 14ec227 32415f0 d845a38 3d1fe45`

---

# 2026-08-16 (2차) — 사용설명서를 대시보드에서 열기

## 무엇이 바뀌었나

1. **corezent.com/manual** 주소가 생겼습니다 — 설명서 HTML 파일을 그대로
   보여줍니다(로그인 불필요). 파일을 아직 안 올렸으면 "준비 중" 안내가 나옵니다.
2. **대시보드 → 라이선스**의 다운로드 버튼 옆에 **「사용설명서 보기」**(회색 테두리
   +책 아이콘) 버튼이 생겼습니다. 지니워크 라이선스에만 나오고, 파일을 올리기
   전에는 숨겨져 있습니다.
3. 파일 올리기는 **관리자 → 설정 → 일반 설정 → "사용설명서 파일"**에서 합니다.

## ★ 대표님이 하실 일 (순서대로 — 이걸 해야 버튼이 나타납니다)

1. Supabase → CoreZent 본체 프로젝트 → SQL Editor →
   `supabase/migrations/064_storage_manuals.sql` 내용 전체를 붙여넣고 Run
   (설명서 보관함을 만드는 것 — 이걸 먼저 안 하면 2번이 실패합니다)
2. 관리자 → 설정 → 일반 설정 → "사용설명서 파일" → 「파일 올리기」로
   `사용설명서.html` 선택 → 주소가 채워지면 그 카드의 「저장」 누르기
3. corezent.com/manual 을 열어 설명서가 보이는지(한글·사진 12장)
4. 대시보드 → 라이선스에서 「사용설명서 보기」 버튼 확인

나중에 설명서를 바꿀 때: 같은 자리에서 새 파일 올리고 저장 — 5분 안에 반영됩니다.

## 문제가 생기면

| 증상 | 조치 |
|---|---|
| 파일 올리기가 "보관함이 아직…" 실패 | 위 1번(SQL)을 먼저 실행 |
| /manual이 계속 "준비 중" | 2번에서 「저장」을 눌렀는지 확인, 5분 뒤 새로고침 |
| 전부 되돌리기 | `git revert --no-edit 45af688 635ea8a 8f7c3b8` |

---

# 2026-08-15 (3차) — 조달청 배지 가독성 + 기관 라이선스 발급 화면

## 무엇이 바뀌었나

1. **조달청 배지가 눈에 띄게** — 상품 설명 위 배지의 「조달청 등록」 라벨이 남색·
   굵은 글씨가 되고 번호가 더 크고 진해졌습니다. 네 곳(상품 상세·요금 페이지·
   홈 요금·제품 목록) 전부 같이 바뀌었고, 번호가 없는 상품은 여전히 아무것도
   안 보입니다. (글자 대비 실측: 라벨 7.6:1, 번호 13:1 — 접근성 기준 통과)
2. **관리자 → 기관 발급** 메뉴가 새로 생겼습니다(견적 요청 다음).
   - 원본 도구(HTML 파일)와 똑같은 입력·검산·SQL 3벌 복사 — 같은 값을 넣으면
     SQL이 글자 하나까지 같습니다(3가지 경우로 대조 완료)
   - **4-1. 바로 발급**: [미리보기]로 값을 표로 확인 → [발급] → 확인 창까지
     거쳐야 실제 등록됩니다. 두 표(license_keys·gw_org_licenses)에 한 묶음으로
     쓰여 한쪽만 써지는 경우가 없습니다(008 함수 — 이미 실행하셨습니다)

## 확인 목록

- https://www.corezent.com/product/geniework — 설명 위 조달청 배지가 진해졌는지
- 관리자 → 기관 발급 — 원본 HTML 파일과 **같은 값을 넣어 등록 SQL이 똑같이
  나오는지 직접 대조**해 보세요(등록 SQL 칸을 나란히 놓고 비교)
- ★ **[발급] 버튼은 진짜로 등록됩니다** — 시험 삼아 누르지 마세요.
  실제 계약 건이 생기면 그때 쓰시면 됩니다. 발급하면 작업 기록(/admin/activity)에
  "기관 라이선스 발급"으로 남습니다(키는 앞부분만)

## 문제가 생기면

| 증상 | 조치 |
|---|---|
| 배지만 되돌리기 | `git revert --no-edit a383dd3` |
| 기관 발급 화면 전부 | `git revert --no-edit cc0d24f d8f7676 dfcffda` |
| 발급 중 "응답이 끊겼지만…" 안내 | 재발급하지 말고 화면의 확인 SQL로 먼저 대조 |

---

# 2026-08-15 (4차) — 소셜 가입도 흐름 측정에 잡히게

## 무엇이 바뀌었나

1. 카카오·깃허브·구글·네이버로 **처음 가입한 경우**가 이제 가입 수(sign_up)에
   잡힙니다(어느 방식으로 가입했는지 함께). 기존 회원의 로그인은 세지 않습니다 —
   추천 적립에 이미 쓰이는 "방금 만들어진 계정인지" 판정을 그대로 썼습니다.
2. 같은 브라우저에서 가입 직후 다시 로그인해도 두 번 세지 않습니다(10분 잠금).
3. 겸사겸사: 결제 완료(purchase) 측정이 도구가 늦게 실리면 유실되던 문제도
   함께 고쳤습니다(같은 원인).

## 확인 목록 (★실제 소셜 계정으로 가입해야 확인됩니다)

- 시크릿 창에서 **한 번도 안 쓴 소셜 계정**으로 가입 → 가입이 평소처럼 끝나는지
  (측정은 뒤에서 조용히 — 화면 변화 없음)
- 기존 계정으로 소셜 로그인 → 평소와 완전히 같은지
- 수치는 구글 애널리틱스 → 보고서 → 참여도 → 이벤트에서 **보통 하루 뒤**
  sign_up의 method 값(kakao·github·google·naver·email)으로 나뉘어 보입니다
  (실시간 보고서에서는 몇 분 안에 보이기도 합니다)

## 못 막는 경우 (알고 계시면 되는 한계)

- 가입 5분 안에 **다른 기기·다른 브라우저**로 다시 로그인하면 드물게 한 번 더
  세질 수 있습니다(잠금이 브라우저 단위라서). 같은 브라우저는 확실히 막습니다.
- 같은 브라우저에서 10분 안에 **다른 사람이 진짜로 가입**하면 그 건은 안 세집니다
  (10분 지나면 정상). 정확도와 중복 방지 사이의 절충입니다.

## 문제가 생기면

| 증상 | 되돌리는 명령 |
|---|---|
| 이번 작업 전부 | `git revert --no-edit 1e4f313 343b9c2` |
